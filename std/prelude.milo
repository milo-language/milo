// std/prelude — auto-imported into every Milo program (suppress with --no-prelude)

from "std/string" import {
    strContains, strIndexOf, strIndexOfFrom, strLastIndexOf, strStartsWith, strEndsWith, strToLower, strToUpper, strTrim, strTrimStart, strTrimEnd, strSplit, strSplitWords, strSplitWhitespace, strRepeat, strReplace, strReplaceFirst, strPadStart, strPadEnd, strIsEmpty, strCharAt, strReverse, strParseInt, charIsWhitespace, charIsDigit, charIsAlpha, charIsAlphanumeric, trim, vecJoin
}
