// @runtime-error: value out of range

pub type SmallInt = i32(0..100)

pub type TinyInt = i32(0..50)

pub fn main(): i32 {
    let a: SmallInt = 30
    let b: SmallInt = 40

    // a(0..100) + b(0..100) = (0..200) does NOT fit in TinyInt(0..50) — needs runtime check
    let sum: TinyInt = a + b
    print(sum)
    return 0
}
