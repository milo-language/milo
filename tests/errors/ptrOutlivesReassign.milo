// @error: 'v' is reassigned here while 'p' still points into its buffer (from 'v.ptr()' on line 9)
from "std/os" import {
    strlen
}

pub fn main(): i32 {
    var v: Vec<u8> = Vec.new()
    v.push(0)
    let p = v.ptr()
    v = Vec.new()
    print(strlen(p))
    return 0
}
