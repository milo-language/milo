// @expect: 42

from "std/sync" import { Mutex }

fn main(): i32 {
    let m = Mutex.new()!
    var x: i64 = 0
    m.withLock((): void => {
        x = 42
    })!
    print(x)
    m.destroy()
    return 0
}
