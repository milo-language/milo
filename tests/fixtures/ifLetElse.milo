// @expect: got 10
// @expect: was none

enum Option {
    Some(i32),
    None,
}

fn main(): i32 {
    let a = Option.Some(10)
    if let Option.Some(v) = a {
        print("got ", v)
    } else {
        print("was none")
    }

    let b = Option.None
    if let Option.Some(v) = b {
        print("got value")
    } else {
        print("was none")
    }

    return 0
}

