// @expect: hello world
// @expect: done
// tests: returning string transfers ownership, callee doesn't double-free

pub fn makeGreeting(name: string): string {
    let result = "hello " + name
    return result
}

pub fn main(): i32 {
    let msg = makeGreeting("world")
    print(msg)
    print("done")
    return 0
}
