// zero-capture closure (null env)
extern fn printf(fmt: *u8, ...) -> i32

fn apply(f: fn(i32) -> i32, x: i32) -> i32 {
    return f(x)
}

fn main() -> i32 {
    let result = apply((x: i32) => x + 1, 41)
    printf("%d\n", result)
    return 0
}
// @expect: 42
