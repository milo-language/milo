// closure capturing an immutable local

pub fn apply(f: (i32) => i32, x: i32): i32 {
    return f(x)
}

pub fn main(): i32 {
    let multiplier: i32 = 3
    let result = apply((x: i32) => x * multiplier, 14)
    print(result)
    return 0
}
// @expect: 42
