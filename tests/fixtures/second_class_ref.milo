// @expect: 42
extern fn printf(fmt: *u8, ...) -> i32

fn double_it(x: &mut i32) {
    x = x * 2
}

fn read_it(x: &i32) -> i32 {
    return x
}

fn main() -> i32 {
    var n: i32 = 21
    double_it(n)
    printf("%d\n", read_it(n))
    return 0
}
