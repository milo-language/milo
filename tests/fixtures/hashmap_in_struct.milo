// @expect: len: 2
// @expect: done

fn make_map(): HashMap<i32, i32> {
    var m: HashMap<i32, i32> = HashMap.new()
    m.insert(1, 10)
    m.insert(2, 20)
    return m
}

fn main(): i32 {
    let m = make_map()
    print("len: %ld", m.len)
    print("done")
    return 0
}
