// @expect: true
// @expect: false
extern fn puts(s: *u8) -> i32

fn main() -> i32 {
    if !false {
        puts("true")
    }
    if !true {
        puts("true")
    } else {
        puts("false")
    }
    return 0
}
