// @expect: called: 42
struct Handler {
    callback: fn(i32): void
}

fn print_val(x: i32): void {
    print("called: %d\n", x)
}

fn main(): i32 {
    let h = Handler { callback: print_val }
    h.callback(42)
    return 0
}
