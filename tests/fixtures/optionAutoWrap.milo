// @expect: is null
// @expect: got 42
// @expect: got 100
// @expect: is null

pub fn check(val: u16?) {
    match val {
        Option.Some(v) => {
            print("got ", v)
        }
        Option.None => {
            print("is null")
        }
    }
}

pub fn main(): i32 {
    var x: u16? = null
    check(x)
    x = 42
    check(x)

    let y: u16? = 100
    check(y)

    let z: u16? = null
    check(z)
    return 0
}
