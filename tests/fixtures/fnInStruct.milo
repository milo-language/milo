// @expect: called: 42

struct Handler {
    callback: (i32) => void
}

fn printVal(x: i32): void {
    print("called: ", x)
}

fn main(): i32 {
    let h = Handler {
        callback: printVal
    }
    h.callback(42)
    return 0
}

