// block closure with explicit return type

pub fn apply(f: (i32) => i32, x: i32): i32 {
    return f(x)
}

pub fn main(): i32 {
    let result = apply((x: i32): i32 => {
        let doubled = x * 2
        return doubled + 1
    }
    , 20)
    print(result)
    return 0
}
// @expect: 41
