// @expect: got: hello
// @expect: done
// @expect: drop: hello
// tests: var assigned + returned via enum variant drops exactly once

pub struct Wrapper {
    value: string,
}

impl Drop for Wrapper {
    fn drop(self: &mut Self): void {
        print("drop: ", self.value)
    }
}

pub fn tryMake(): Wrapper {
    var conn = Wrapper {
        value: "hello"
    }
    return conn
}

pub fn main(): i32 {
    let val = tryMake()
    print("got: ", val.value)
    print("done")
    return 0
}
