// @expect: greeting: hello world!

pub fn greet(name: string): string {
    return "hello " + name + "!"
}

pub fn main(): i32 {
    let msg = greet("world")
    print("greeting: ", msg)
    return 0
}
