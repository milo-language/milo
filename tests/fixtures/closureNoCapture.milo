// zero-capture closure (null env)

fn apply(f: (i32) => i32, x: i32): i32 {
    return f(x)
}

fn main(): i32 {
    let result = apply((x: i32) => x + 1, 41)
    print(result)
    return 0
}
// @expect: 42

