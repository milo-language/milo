// @expect: 42
// @expect: error: too small

fn double_if_big(x: i32): Result<i32> {
    if x < 10 {
        return Result.Err("too small")
    }
    return Result.Ok(x * 2)
}

fn main(): i32 {
    let a = double_if_big(21)
    match a {
        Result.Ok(v) => { print("%d", v) }
        Result.Err(msg) => { print("error: %s", msg) }
    }

    let b = double_if_big(5)
    match b {
        Result.Ok(v) => { print("%d", v) }
        Result.Err(msg) => { print("error: %s", msg) }
    }

    return 0
}
