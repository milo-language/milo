name: Deploy docs

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      # Compile the Milo emulator cores to JS and stage each demo under the
      # VitePress public dir so they deploy at /milo/nes/, /milo/genesis/ and
      # /milo/snes/. Source of truth stays in examples/apps/*/web; the *-core.js
      # files are regenerated here to match their webcore.milo.
      - name: Build Milo browser demos
        run: |
          bun install
          bun run src/main.ts emit-js examples/apps/nes/webcore.milo -o examples/apps/nes/web/nes-core.js
          bun run src/main.ts emit-js examples/apps/genesis/genesisWebcore.milo -o examples/apps/genesis/web/genesis-core.js
          bun run src/main.ts emit-js examples/apps/snes/snesWebcore.milo -o examples/apps/snes/web/snes-core.js
          mkdir -p docs/site/public/nes docs/site/public/genesis docs/site/public/snes
          cp examples/apps/nes/web/index.html examples/apps/nes/web/nes-core.js docs/site/public/nes/
          cp examples/apps/genesis/web/index.html examples/apps/genesis/web/genesis-core.js docs/site/public/genesis/
          cp examples/apps/snes/web/index.html examples/apps/snes/web/snes-core.js docs/site/public/snes/

      - name: Install dependencies
        run: cd docs/site && bun install

      - name: Build
        run: cd docs/site && bun run build

      - uses: actions/configure-pages@v6

      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs/site/.vitepress/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
