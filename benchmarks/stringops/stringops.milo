// concat 100k chunks via string +=
fn main(): i32 {
    let n: i64 = 100000
    let chunk = "the quick brown fox jumps over the lazy dog"
    var buf: string = ""
    var i: i64 = 0
    while i < n {
        buf = buf + chunk
        i = i + 1
    }
    print("len=", buf.len)
    return 0
}
