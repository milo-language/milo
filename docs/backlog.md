<!-- doc-meta
system: planning
purpose: ROI/difficulty-ranked lens over outstanding work; a prioritization view, NOT the source of truth for status
key-files: docs/roadmap.md (canonical status), docs/safety-roadmap.md
update-when: an item ships (flip in roadmap.md, then DELETE it here) or a new item is triaged
last-verified: 2026-09-19 (Tier 1 #35 filed by the soundness sweep's WP4; #33 closed by its @copyOnly package. Earlier, 2026-08-17: Tier 1 #30 and #31 filed, and #7 updated with them, from an audit of what `index-clone` should accept as an escape hatch: the index path ignores `@noCopy` and `Drop`, and `Clone` turns out not to be derivable at all. Earlier, 2026-08-15: three entries filed from a review of astral-sh/hawk, a closed-world visibility lint for Cargo workspaces: Tier 1 #24 `unnecessary-pub` and #25 `expect` warning levels, Tier 2 #20 dead-`pub` analysis over `std/`+`src-milo/`. Earlier, 2026-08-03: linked the stdlib audit tracker — its items live there, not here, so this file stays a compiler/language lens; partial moves now match Rust everywhere except the index row: re-using a moved field, using a partially-moved struct as a whole, and taking a field out of a `Drop` type are all rejected — the last of those used to skip the destructor entirely, a silent resource leak live on main. All three measured against 645 entry points across this repo, the emulators, milojs, yaml, dapweb and the gl/sdl packages before enforcing: 0 rejections. `checker.fatal(): never` landed with per-statement/per-function/per-program recovery boundaries, closing the `error()`-then-continue entry; `scripts/fuzz-ownership.ts` landed and immediately found one: moving out of a struct field twice compiled and returned an empty string, now tracked per place (`VarInfo.movedPlaces`) and merged through branches. Two new entries left behind: borrowing a partially-moved struct, and the fuzzer's single generated shape. Earlier: the eight ad-hoc place-walkers collapsed into one total, fail-closed `placesOf`; two double-frees closed with it — move-out-of-borrow and use-after-move, both through if/match/`??` tails, which no walker recognized. Left behind: the `fatal(): never` residue, now its own entry, and one more spelling reaching Tier 1 #7. Earlier: added three from the visibility/linkage keyword review: package-scoped `pub` (T2, has a deadline — before packages ship), the `@externalLinkage`-without-`pub` lint (T1), and `extern struct` being misnamed (T3); earlier same day: the void-`return` miscompile and print buffering, both found writing examples/games/apsis; earlier: full probe audit: deleted the shipped overflow-default flip, re-scoped the &mut-slice entry to dynamic splitMut only, added the indirect-closure-escape UAF and the generic-static turbofish gap)
-->

# Backlog — prioritized

A **do-next ordering** across the open work. Status source of truth is [roadmap.md](roadmap.md); this file only ranks by return-on-investment vs effort. `Ref` links the canonical entry.

**Refs name things, never line numbers.** Every `roadmap.md:NNN` in here had rotted — `:115` pointed at an unrelated shipped item, `:109`/`:123` at blank lines. A ref that silently points somewhere plausible is worse than none. Same for `#N` cross-references: renumbering after a deletion left one item pointing at itself and another at a deleted entry, so prefer naming the item.

**Verify before working an entry.** These are written from intent and rot as code lands: three entries here (LSP rename, `milo doc`, the parser bug's diagnosis) turned out already shipped or misdiagnosed, and two more (iterators' "needs associated types", Option's "needs closures") named blockers that no longer existed. The full audit then found three more shipped entries still listed (SIGWINCH select arm, shuf/Promise ergonomics, benchmarking harness). Check the code first; correct the entry when it lies.

**Stdlib work lives in its own tracker.** The 2026-08-03 comparative sweep against Go/Rust/Node is [stdlib-audit-2026-08.md](stdlib-audit-2026-08.md) — checkbox items, tiered the same way. It is not duplicated here; this file stays a compiler/language lens. Three of its entries name a blocker that IS here (generic-static turbofish → T1 #5, package-scoped `pub` → T2 #1b, lazy iterators → T2 #6, declined).

**Shipped items are deleted from this file, not struck through** — git history and roadmap.md keep the record. If an entry ships but leaves real work behind, only the leftover survives here, rewritten as its own item.

ROI / Effort: **H**igh / **M**edium / **L**ow. Tiers = the quadrant that matters: ship Tier 1 first (cheap + high payoff), invest deliberately in Tier 2, let Tier 3 wait.

## Tier 1 — quick wins (high ROI, low effort) — do first

| # | Item | ROI | Effort | Why / unblocks | Ref |
|---|------|-----|--------|----------------|-----|
| 32 | **`std/json`'s brand mint reaches `addrOf`, so every `@derive(Json)` program lost the JS backend** | M-H | L-M | **FOUND 2026-08-31 while refreshing the emit-js parity baseline after the FFI work.** The document-brand counter is minted with `_atomicAddI64((nextJsonDocId.addrOf()) as *u8, 1)` (`std/json.milo:77`), and `addrOf` is one of the constructs `emit-js` refuses, so ANY program importing `std/json` now fails to emit JavaScript. The parity baseline grew by 23 entries in one refresh - the seven `deriveJson*` fixtures among them - and every one is a program that used to run in the playground and no longer does. The baseline is documented as a list that may shrink and never grow, so this growth is the gate reporting a real loss rather than noise. The atomic is there for thread safety and is right; what is wrong is that the JS backend has no answer for it, even though a single-threaded backend's answer is trivially `++`. Two ways out: teach `codegen-js` the `addrOf`-of-a-global + `_atomicAddI64` shape (narrow, and it would recover `std/seal`'s counter too), or give the compiler an intrinsic for 'atomically bump this global' that each backend lowers its own way (wider, and it removes the `addrOf` from readable stdlib source). Until one lands, the playground cannot run any JSON program. | `std/json.milo:77`, `std/seal.milo:98`, `src/codegen-js.ts`, `tests/emitJsParity.baseline.json` |
| ~~33~~ | ~~**A brand-mismatch diagnostic names the monomorphized instance, not the type the user wrote**~~ | M | L | **CLOSED 2026-09-19 with `@copyOnly` (soundness sweep WP2).** Every checker diagnostic now formats types through `show`, which threads a demangler into `typeName`: `Id_UserTag` prints as `Id<UserTag>`, `Result_JwtClaims_JwtError` as `Result<JwtClaims, JwtError>`, recursively. `typeName` without the demangler stays the identity for lookups. Pinned by `tests/errors/brandMismatchNamesWrittenType.milo`. Original entry: **FOUND 2026-09-01 while evaluating witness/brand idioms for [proofs-vs-contracts](proofs-vs-contracts.md).** Passing an `Id<OrderTag>` where an `Id<UserTag>` is wanted reports `expected Id_UserTag, got Id_OrderTag`. The mangled instance name is an implementation detail of monomorphization and the user never wrote it; the message should say `Id<UserTag>` / `Id<OrderTag>`. Phantom brands are the idiom most likely to produce this error on purpose - the whole point of a brand is that the compiler rejects the mixup - so it is the diagnostic a reader meets exactly when they are doing the right thing. Demangling for display is local to the type formatter. | `src/checker.ts` (typeName / mismatch messages), `src/diagnostics.ts` |
| 35 | **A non-void fn with no `return` on some path is not rejected by the checker** | M | M | **FOUND 2026-09-19 by the WP11 reducer.** There is no all-paths-return analysis at all. `pub fn main(): i32 { print("x") }` compiles and exits 0 (codegen's deliberate "main's implicit 0", which it also applies to EVERY `i32` fn: `fn f(): i32 { print("x") }` silently returns 0). Any other return type reaches clang: `fn f(): i64 { print("x") }` fails with an LLVM "block must end with a terminator" error, which violates CONVENTIONS.md's "semantic errors are caught in checker.ts". Repro: `fn f(): i64 { print("x") } pub fn main(): i32 { print(f()); return 0 }` under `milo run`. Fix: a divergence walk over the body's last statement (return / abort / `while true` without break / if-else and match whose every arm diverges), reusing `tailExprOf`'s arm logic; decide whether `main` keeps its implicit `0` (document it in language-reference if so). | `src/checker.ts` checkFunction, `src/codegen.ts` genFunction |
| 36 | **The import list validates but never restricts: `from "std/os" import { }` brings every `std/os` name into scope** | L-M | M | **FOUND 2026-09-19 by the WP11 reducer.** `src/resolver.ts` merges the whole imported module after checking that each listed name exists ("named imports validate but don't restrict (flat compilation)"), so `from "std/os" import { getenv }` also makes `strlen` callable, and the empty list is the degenerate case. Repro: `from "std/os" import { } fn main() { print(strlen("hi")) }` type-checks. A real fix is a per-file visible-name set consulted by the checker's name lookup (functions, types, globals; methods stay reachable through their receiver), with the flat namespace kept for codegen; the empty-list spelling could be rejected at the parser as a one-liner but would only hide the general case. | `src/resolver.ts` (the merge after the `is not exported by` check), `src/checker.ts` lookup |
| 34 | **A generic struct literal refuses explicit type arguments** | L-M | L | **FOUND 2026-09-01.** `And<i64, bool> { left: 1, right: true }` is rejected with `'And<i64, bool> { … }' is not a struct literal`, and the hint names the two ways through (infer from the fields, or annotate the binding). The hint is good and the restriction is real, but the turbofish is what a user reaches for first when inference cannot see the parameter, and generic FUNCTION calls accept exactly that spelling - `orIntroLeft<i64, bool>(7)` is how you fix an uninferable parameter there. One construct taking the annotation and its sibling refusing it is the inconsistency, not the restriction itself. | `src/parser.ts` (struct literal path), `src/checker.ts` |
| 0 | **Branch-and-bound for the remaining rational-only unknowns** | H | M | **Re-measured 2026-07-25 after the float pass: 20 of 108 tree-wide unknowns — no longer the largest cause (missing callee `ensures` ~58 and `FieldAccess` 35 both outrank it), and it is a SOLVER gap, not a theory gap; `--solver=z3` already answers most of them.** Fourier-Motzkin decides feasibility over the RATIONALS, so a system like `2x = 3` looks satisfiable (x = 3/2) while having no integer solution; `std/smt` can then neither refute it nor produce a witness an i64 could take, and reports `no integer witness (rational-only)`. The cheap half shipped in f38c0ea2 — `reduceConstraint` folds strictness away (`L < 0` on integer L is `L + 1 <= 0`) and rounds the constant in by the coefficient gcd, both exact over Z — which took 32 -> 29. The rest needs true branch-and-bound: take a fractional coordinate `x = p/q` and recurse on `x <= floor(p/q)` and `x >= ceil(p/q)`. **The blocker is that FM produces no witness/vertex to branch ON** — it answers feasibility only. Either extract per-variable bounds during elimination, or replace the relaxation with a simplex that yields a vertex. Bounded depth; exhausting it must still report `unknown`. | `std/smt.milo feasibleRational`, `decide`, `src/prove-milo.ts` |
| 1 | **`assert E` as a proof cut** | H | L | **SHIPPED 2026-08-16.** Needed no new syntax — `assert` was already a builtin, so the whole change is in `verify.ts`: each statement-level `assert(E)` becomes a VC at that program point, and E is pushed onto the path conditions so everything downstream assumes it. Sound because codegen aborts unconditionally on a false assert in every build mode, which is exactly what a bare `assume` cannot claim (and why there still is none). One thing the entry did not anticipate and that turned out to matter: a cut whose own VC is REFUTED still feeds the assumption, so `assert(a >= 100)` failed with a counterexample while the postcondition it made trivial reported a clean tick. Cuts now carry an identity and dependents name it (`assumesCuts`), reported through the same `conditionalProofs` path a callee's `ensures` uses. Measured: `sumTo`'s postcondition goes from REFUTED (the loop havocs `s`) to proven. Known non-win, documented rather than filed: a cut cannot rescue a nonlinear TERM — `let a = w * h; assert(a >= 1)` lowers `a` back to `(* w h)` on both sides and std/smt rejects the row either way; that needs subterm abstraction, a different change. | `src/verify.ts` (collectPaths, conditionalProofs), `tests/prove/proofCut.milo`, `tests/prove/proofCutRefuted.milo` |
| 2 | **Enforce range subtypes, THEN propagate them into the prover** | H | M | **(a) ENFORCEMENT SHIPPED 2026-07-30** — ranged-int subtypes (`i32(0..100)`) are now enforced at call arguments, `return`, and reassignment, not just `let`/`var`. `enforceRangeInto` (`checker.ts`) rejects an out-of-range *literal* statically (errors `rangeArg|rangeReturn|rangeReassign`), and a non-literal out-of-range value traps at runtime via a `HIR RangeCheck` node that wraps the *value expression* (so every position emits `emitRangeCheck`, not only `let`/`var`) — `runtime-errors/rangeRuntime.milo`. **What's LEFT is (b):** feed the now-guaranteed ranges to the prover — `intRangeAssumption` can read `rangeMin`/`rangeMax` instead of only the type's width. Note (b) alone does NOT retire the `pidStep` baseline: `error` there is an unannotated `i32` local with no declared range — retiring it needs range-carrying arithmetic (Tier 2 #3) or an annotation. | `checker.ts enforceRangeInto`, `hir.ts RangeCheck`, `verify.ts intRangeAssumption`, `scripts/verify-contracts.baseline.ts` |
| 2c | **`FieldAccess` in call-site substitution** | H | M | **Now the largest TRANSLATOR cause: 35 of 108 unknowns (32 in `flightController` alone), measured 2026-07-25.** `flattenFieldAccess` only names a field path whose root is a plain `Ident`, and in a foreign (call-site) lowering anything else becomes `(UNSUPPORTED FieldAccess)` — so a precondition mentioning a parameter's field goes unchecked whenever the caller's argument is not a bare variable. Cheaper than it looks relative to its count, but the exact failing shapes have NOT been enumerated yet: diagnose which callers hit it before designing the fix, since a wrong rebase silently checks a different obligation (the bug `rebaseFieldAccess` was written for). | `src/verify.ts flattenFieldAccess`, `rebaseFieldAccess`, `exprToSmtWithEnv` |
| 1c | **A guard/SIGKILL-ed program loses its buffered stdout** | M | L | **RE-SCOPED 2026-08-15 — two of the three cases in the original entry are already correct.** On a TTY `print` IS line-buffered (probe: print-then-spin, killed at 3 s under `script -q`, printed), and `panicAbort` fflushes (probe: print-then-index-out-of-bounds through a pipe, printed). What is still lost is a SIGKILL with a non-TTY stdout — exactly the agent debugging loop: `bun scripts/guard.ts --timeout-s 3 -- ./hang` emits `[guard] SIGKILL: exceeded 3000 ms` and NOTHING the program printed. Block-buffering on a pipe is deliberate and documented (`codegen.ts emitStdoutWrite`: print used to cost fflush+write(2) per line, which dominated piped loops) — so do NOT force line buffering globally. The bounded fix is an opt-in the killer can set: honour `MILO_LINE_BUFFERED` at startup (`setvbuf(stdout, NULL, _IOLBF, 0)`) and have `guard.ts` / `milo run` set it for the children they may kill. Costs nothing when unset and turns "no output at all" into "output up to the hang" precisely where a watchdog is involved. Debugging an infinite loop in a graphical program was therefore impossible by instrumentation: the fix was to attach `sample(1)` to the live process and read the stack. Line-buffer when stdout is a TTY (and flush on abort/panic), which is what every other runtime does. Cheap, and it turns "no output at all" into "output up to the hang". | `std/io.milo`, runtime print path |
| 5 | **Generic-struct statics need a turbofish** | M | L-M | `impl Pair<A, B> { fn new(…) }` then `Pair.new(1, 2)` **CLOSED 2026-08-16 — the inference half now ships.** A bare `Pair.new(1, "x")` recovers its type arguments by unifying the argument types against the generic impl's own parameter list (`checker.ts inferGenericStaticTypeArgs`), then hands the call to the existing turbofish path, so both spellings resolve to the same monomorphization. Type parameters nested inside a container are recovered too (`Wrap.of(v)` binds `T` from a `Vec<i64>`), and the calls compose (`Pair.new(Pair.new(1, 2), "deep")`). Real std win, not just the synthetic case: `BufReader.new(f)` compiles, which `docs/stdlib-audit-2026-08.md` had recorded as a permanent turbofish. **Still requires the turbofish when no argument mentions the parameter** — `HashSet.new()`, `Empty<T>.make()` — where there is nothing to infer from; that shape keeps the existing "spell its type arguments" hint, so it is a narrower error rather than a removed one. One diagnostic improvement fell out: an argument that fails to type (`Pair.new(1, typo)`) now reports `undefined variable 'typo'` instead of blaming the call with `no static method 'new'`. Locked by `tests/fixtures/genericStaticInfer.milo`. The 2026-08-15 note below is the diagnostic half that preceded it. **RE-VERIFIED 2026-08-15 — the diagnostic half has SHIPPED, the inference half has not.** `Pair.new(1, 2)` no longer says `unknown enum 'Pair'`; it now hints `'Pair' is generic — spell its type arguments: 'Pair<A, B>.new(...)'`, so the error is discoverable and the workaround is in the message. The call still FAILS, and the follow-on `cannot access field 'a' on type <unknown>` is noise from the same root. Remaining work is only the second half below: infer the type params from the arguments (or the LHS annotation) so the bare form compiles. `Pair<i64, i64>.new(…)` works, instance methods work bare, and non-generic statics (`Crypto.sha256()`, `DateTime.now()`) are fine; `Vec.new()` only works because Vec is a builtin with special inference. This is why `std/arena` exposes free `arenaNew<T>()` instead of `Arena.new()`, and it blocks the `Type.new()` convention from the namespace-object model for every generic collection. Fix is two halves: don't treat `Type.method` as enum access when `Type` names a struct, and infer the type params from the LHS annotation. | `checker.ts` (FieldAccess/enum-variant resolution), `std/arena.milo` |
| 6 | **The move-out-of-borrow exemption is keyed to a NAME, not a property** | M | M | **RE-VERIFIED 2026-08-16, still exactly as described.** `v.sortByKey((u: &User) => u.name)` compiles; the identical closure handed to a user's own non-retaining combinator (`fn pick(v: &Vec<User>, key: (&User) => string)`) is rejected with `cannot move 'u.name' out of the borrowed 'u'`. So the asymmetry is live: std gets an ergonomic std's users cannot have, and `retainsParam` computing the property for user fns has not reached this rule. `sortByKey`'s key extractor is the sole exemption from *"cannot move a non-Copy field out of a borrowed value"* (`checker.ts`, `keyExtractorDepth`), and it is exempt for a real reason: the extractor does not **retain** what it returns, so the field is compared and dropped. `map` does retain — `users.map((u: &User) => u.name)` built a `Vec<string>` aliasing the users' buffers and double-freed (live abort, exit 133) — which is why exempting closures generally was unsound and why the current rule is fail-closed. **The problem is the key: a std function name.** A user writing their own non-retaining combinator cannot obtain the same treatment, so std gets ergonomics user code can't have, which is the asymmetry Milo otherwise avoids. Generalizing means naming the *property* (this callback's return value is not retained) rather than the function — closely related to `@pure` (Tier 1 #3), and probably the same annotation machinery. Until then the carve-out is correct but not principled, and every new combinator is subject to the rule by default.  **DISPROVED 2026-08-16 — the mechanism this entry proposes does not work, and building it produces a use-after-free.** The 2026-08-14 note below is about a DIFFERENT property. `retainsParam` asks whether a callee keeps the fn VALUE (closure escape); this rule needs whether it keeps the fn's RESULT. I wrote the missing one (`retainsCallbackResult`: walk the callee body, answer transient only for a call bound to a read-only local or compared inline, fail-closed everywhere else, forwarding resolved transitively), wired it to the user-fn call path, and it did exactly what this entry asks: `pick(v, (u: &User) => u.name)` compiled, a user `map` that pushes the key stayed rejected, forwarding to a retaining callee stayed rejected, recursion stayed rejected. Then ASan: **heap-use-after-free in `pick`**. **Retention was never the property.** `pick` does not store the key — it compares it and lets it go, and letting it go IS a free: `let a = key(v[i])` binds an OWNED string, so it carries ordinary scope-end drop glue, and the buffer it drops belongs to the `User` the closure merely borrowed. `sortByKey` is exempt not because it fails to retain but because **a builtin is the one kind of callee that can promise never to drop what it was handed** — it has no Milo body, hence no drop glue, which is the same fact that makes it un-analysable. So the two halves of the carve-out are the same half. Closing this needs the callee to hold the result WITHOUT owning it — a borrowed return — and second-class references cannot return one. That makes this entry blocked on the reference model, not on an annotation or a table, and it should not be re-attempted as one. Locked by `tests/errors/keyExtractorUserCombinator.milo`, which carries the reasoning so the next reader does not rebuild the same unsound analysis. **UPDATE 2026-08-14 — half of this now exists (and is the other property, see above).** `checker.ts retainsParam` computes exactly the property this entry asks for (does this callee keep its fn-typed argument?) from the callee's body, and `checkEscapingClosures` uses it so a user's own non-retaining combinator gets the same treatment std's does. What it does NOT cover is `sortByKey` itself: a builtin has no Milo body to analyse, so `keyExtractorDepth` is still keyed to the name. Closing this entry means giving builtins the same property — a table or an annotation on the declaration — not inventing the analysis, which is written. | `checker.ts keyExtractorDepth`, `checker.ts retainsParam`, `tests/errors/keyExtractorUserCombinator.milo`, `tests/fixtures/sortByKeyString.milo` |
| 7 | **One aliasing rule, three answers depending on the container** | M | L-M | **A FOURTH answer found and CLOSED 2026-08-16, and it was a use-after-free rather than a teaching problem.** `for x in v` froze the iterated Vec; `for x in b.items` froze NOTHING, because the freeze fired only when the iterable was a bare `Ident` (`checker.ts` ForInStmt). So a Vec reached through a field could be reallocated inside its own loop — directly (`b.items.push(...)`) or through any function taking the struct as `&mut` — while the loop held a pointer into the old buffer. Both spellings were **heap-use-after-free in safe code with zero `unsafe`**, confirmed under ASan. Fixed by freezing the ROOT of whatever place is iterated (`accessPath`), which covers both spellings at once; the recorded path is what keeps it from over-rejecting, so mutating a DIFFERENT field of the same struct still compiles. Found because milo-self rejected this shape in the compiler's own source while the oracle accepted it — the self-hosted checker was stricter than the checker that built it. Locked by `tests/errors/forInFieldMutate.milo` and `tests/errors/forInFieldMutateThroughFn.milo`. **RE-VERIFIED 2026-08-16, all three answers still live and measured on one program**: `return b.v` (field out of a borrow) is a hard error naming `.clone()`; `return b.v[0]` (index) silently deep-copies; `return if c { b.v[0] } else { b.v[1] }` (fork of index) silently deep-copies both tails. **The `index-clone` lint now covers the last two** (2026-08-16) — it previously only fired where a value was BOUND, so the exact spelling this entry is about was invisible even with the lint on. Census went 26 -> 33 hits across src-milo and std, i.e. 7 real allocation sites that no gate mentioned. Still opt-in, and still a diagnostic rather than the semantic decision this entry asks for. **One more spelling reaches it as of 2026-08-03**: `if c { v[0] } else { v[1] }` on a `&Vec<string>` compiles and clones, because a fork now correctly forwards the move rule to its tails and the *index* tail is the branch that clones. Not a new hole — the same silent-clone row, one more way to ask for it, which is the argument for making the row uniform rather than adding a fourth answer. Moving a non-Copy element out of a borrow is a hard ERROR for a field (`cannot move 'b.v' out of the borrowed 'b'`, with a `.clone()` hint) but a SILENT deep clone for an index (`v[i]`) — and until 2026-08-01 a silent DOUBLE FREE for a *sized* array, where the index path loaded in place so the copy and the array both freed the same buffer: `fn peek(a: &[string; 2]): string { return a[0] }` aborted at exit 133 with zero unsafe in the program. The hole is closed (sized arrays now clone like Vec — `codegen.ts indexAccessClones`, `tests/fixtures/array_index_clone.milo`), but the TEACHING is still inconsistent: one conceptual operation errors in one spelling and silently allocates in another, so a reader cannot infer the rule from either, and the next container added inherits whichever branch it happens to hit. Decide the language's answer — error-with-hint everywhere (consistent; costs the `v[i]` ergonomics a lot of code leans on) or clone-everywhere (consistent; silently allocates inside loops) — and make the other spelling match. Related: #6, which is the same rule carved out by function NAME rather than by property. **A FIFTH answer, found 2026-08-17 and filed as #30, and the first that is a memory-safety bug rather than a teaching one:** the index path copies memberwise without consulting `@noCopy` or a `Drop` impl, so `let a = v[0]` on a `Vec<Texture>` (`@noCopy`) or a `Vec<File>` (`Drop` closes the fd) duplicates a handle and releases it twice, with a warning and no error. That decides this entry for at least one slice: for elements carrying `Drop` or `@noCopy`, error-with-hint is not merely the consistent choice but the only correct one, and the general `v[i]` clone can stay. Which hint it can name is blocked by #31, since `Clone` is not derivable and `.clone()` does not compile for most struct types. | `checker.ts` (field move-out-of-borrow), `codegen.ts indexAccessClones`, `tests/fixtures/array_index_clone.milo` |
| ~~8~~ | ~~**`let m = v[i]` on a struct with heap fields silently deep-clones**~~ | H | L | **DIAGNOSTIC SHIPPED 2026-08-15 as the opt-in lint `index-clone` (`--deny=index-clone`); the semantic half stays open under #7.** Re-verified first: the index loop still emits one `milo.clone.struct.Mark` and the `for`-in loop over the same Vec emits zero. The lint fires on a `let`/`var` whose initialiser is an `IndexAccess` with a non-Copy element, and names the cheap spellings (`for x in v`, or a field read `v[i].n`, which materialises no element). **Was off by default on that census; turned ON 2026-08-17.** The census argument (26 hits across `src-milo`, 3 in `examples/games/flight`, "noise that gets a lint switched off wholesale") assumed the hits were a mix of real costs and working code paying an accepted price. Re-measured on the shipped lint, they are not a mix: `isCopy` already skips register copies, so a `Vec<Pod>` bind is silent and every hit is an element that owns heap. 33 in `src-milo`, 25 across `examples/`, and the largest single group is 6 in `src-milo/parser.milo` — `peek`, `peekN` and `advance` each return a `Token` (a `string` plus two `Vec<Trivia>`) BY VALUE out of the token stream, so the self-hosted parser mallocs three times per lookahead, at 54 call sites. A cost that large hidden behind syntax that looks free is the case for a default, not against it; measured in isolation the by-value spelling costs ~62 ns/call against a field read's ~0. `--allow=index-clone` remains for a project that wants the copies. Unit tests in `tests/indexCloneLint.test.ts` pin both directions, including that `for`-in and `v[i].n` are never flagged (if they were, the hint would have nothing to point at). |
| 9 | **No default field values — adding a field means editing every literal** | M | M | Adding `found`/`took` to `Mark` in `examples/games/flight` meant touching 25 struct literals or writing a `mk()` constructor by hand (what actually happened). Struct literals are exhaustive with no escape: no field defaults, no `@derive(Default)`, no `..base` struct-update syntax — `@derive(Eq)` is the only derive that exists. The cost lands exactly when a type is still moving, which is when a language most wants to be easy to change. Three candidate shapes, pick ONE: (a) `@derive(Default)` + `Mark { name: n, ..Default }` — most Rust-like, needs a Default trait and a spread form in literals; (b) inline field defaults `struct Mark { found: bool = false }` — most TS-like, cheapest to parse, but puts an expression in a type declaration and raises when it evaluates; (c) `..base` update syntax alone, no defaults — composes with existing constructors and adds no new evaluation rule. Note the interaction with move semantics: `..base` must move-or-copy field by field, and a non-Copy field consumed from `base` leaves `base` partially moved, which the move checker has no representation for today. That interaction is the real work, not the syntax. | `parser.ts` struct literals, `checker.ts` exhaustiveness + move checking, `docs/language-reference.md` |
| ~~10~~ | ~~**Lint `@externalLinkage` on a non-`pub` fn**~~ | L | L | **SHIPPED 2026-08-15 as `external-linkage-not-pub`, on by default.** The entry called it correctly: a couple of lines in the existing `externalLinkage` arm, where `fn.isPub` is already in hand. On by default rather than opt-in, on the census: **0 hits across src-milo, std and the examples** out of 89 `@externalLinkage` sites, so it nags nobody who is doing it deliberately. Warning, never an error — the dlopen case is real, and the test pins that the program still checks clean. Gated in user code only (`currentFnIsUser`), so an imported library's choices are not the caller's problem. `tests/externalLinkageLint.test.ts`; a `tests/errors/` fixture would have been the wrong home, since that driver requires a non-zero exit and this is a warning. |
| ~~11~~ | ~~**Six std module pairs cannot be imported together — duplicate private helper names**~~ | H | L | **CLOSED 2026-08-15.** The silent-acceptance half closed 2026-08-14 (types and globals); this was the diagnosed-but-unfixed residue. The colliding helpers are renamed module-prefixed — `rotl` -> `_xxRotl`/`_sha1Rotl`, `_hexNibble` -> `_xxHexNibble`/`_uuidHexValue`, `hexNibble` -> `_httpHexValue`, and `quoteArg`/`buildCmdLine` -> `_ptyQuoteArg`/`_ptyBuildCmdLine` on the Windows pty arm, which also drops their stray `pub` so the three pty arms export the same surface again. The Windows pair was a real collision, not a legal merge: the two bodies differ (`cstrCopy` vs an inline malloc/memcpy). The identical trio (`_hexNibble` in hmac/sha256/sha1) and `freeArgv` (process + both pty arms) are byte-identical and still merge by design, so they are left alone. Locked by `tests/modules.test.ts` "the std modules with historically colliding private helpers import together" — one program importing the whole hazard set, which catches any future collision in a single compile (verified to FAIL with the renames stashed). The underlying cause is Tier 2 #18 (per-module namespaces); until that lands, a new private helper in std is still one shared name. `src/resolver.ts` now runs the `duplicate-fn` check over two more namespaces at merge time: `duplicate-type` (structs/enums/traits/interfaces/type aliases share ONE namespace — that is why a struct collided with an enum) and `duplicate-global` (globals share the *value* namespace with fns; both become `@name` in the IR, so the only prior signal was clang rejecting generated IR with `redefinition of function '@asciiIsDigit'`). Same tolerance as fns — byte-identical definitions in two files still merge, so vendoring a shared type stays legal. Both new diagnostics name both files and both kinds: `'Response' is defined as a struct in 'std/fetch.milo' and as an enum in 'std/http.milo'`. std renames that followed: `std/mem`'s `Arena` -> `Bump` (`arenaNew`/`arenaAlloc`/`arenaReset`/`arenaRemaining` -> `bump*`; `std/arena` keeps `Arena<T>`), and `std/crypto`'s private `sha256`/`sha1`/`sha1Bytes` -> `cryptoSha256`/`cryptoSha1`/`cryptoSha1Bytes` on all three platform arms (`Crypto.*` method names unchanged). **This entry's census of 16 was wrong and is corrected here:** exactly ONE of the 16 was silent — the `Arena` *type*. Five (`arenaNew`, `arenaAlloc`, `sha256`, `sha1`, `sha1Bytes`) were already hard `duplicate-fn` errors, and so are five of the ten "identical helpers that work by luck" — verified by compiling a program that imports both modules in each pair: `_hexNibble` (hmac/sha1/sha256/uuid/xxhash), `hexNibble` (hex/http), `rotl` (sha1/xxhash), and `quoteArg`/`buildCmdLine` (process/pty, windows arm only). Only `lenBase`/`lenExtra`/`distBase`/`distExtra` (deflate/inflate) and `freeArgv` (process/pty) are genuinely identical and merge by design. The census also missed globals entirely (its regex matched only struct|enum|fn|type|trait): `MASK32` (sha1/sha256) is identical and legitimately merges. **What's LEFT** is those six pre-existing `duplicate-fn` pairs, which make real module combinations unimportable (`std/sha1`+`std/xxhash`, `std/hex`+`std/http`, any two of hmac/sha1/sha256/uuid/xxhash, `std/process`+`std/pty` on Windows). They are diagnosed, not silent, and each needs a rename to a module-prefixed helper name. | `src/resolver.ts` (merge), `std/mem.milo`, `std/crypto.*.milo`, `tests/modules.test.ts` |
| 13 | **`@derive(Json)` has no form for `[T; N]`** | M | **CLOSED 2026-08-16, and verifying it turned up three silent type bugs that had nothing to do with JSON.** The derive now encodes a fixed array exactly as it encodes a Vec (both iterate, both emit a JSON array) and decodes it by building the slots from a zero value and filling by index. Decode REJECTS a wire array of the wrong length rather than truncating or zero-padding it, because accepting one hands the caller a slot the sender never wrote. Element types with no zero (a struct) are refused with a sentence naming `Vec<T>` instead. Locked by `tests/fixtures/deriveJsonFixedArray.milo`. **The three bugs found while testing it, all the same shape — a type annotation the language cannot represent was accepted and silently meant something else:** (1) the parser built an array type from its inner type's NAME alone, so `[[i64; 2]; 2]` was accepted and meant `[i64; 2]`; a flat literal satisfied it, and a nested one reached clang as a store of `[2 x i64]` into an `i64` slot. (2) The same drop made `[&string; 2]` mean `[string; 2]`, an OWNED array, though references are second-class and an array of them has no representation. (3) `resolve` handled a generic element's type ARGUMENTS and returned the element while dropping the array, so `[Vec<i64>; 2]` resolved to a plain `Vec<i64>` and the reported error was "cannot iterate over type 'i64'" against a program that never wrote one. (1) and (2) now say so at the annotation; (3) resolves correctly. Fixtures: `tests/errors/nestedFixedArray.milo`, `tests/errors/arrayOfRefs.milo`, `tests/fixtures/arrayOfGenericElements.milo`. M | **HashMap SHIPPED 2026-08-15; only the fixed-array half is left, and this entry is retitled to it.** `HashMap<string, V>` now encodes as a JSON object and decodes back, composing with the existing plans (`HashMap<string, Vec<i64>>`, `HashMap<string, SubStruct>`, `Option<HashMap<string, string>>` all round-trip). Three things it needed. (a) **Sorted keys, and not for tidiness**: Milo's HashMap iterates in a different order on every RUN of the same binary — verified, three runs, three orders — so an unsorted encoder emits different bytes each time, which is a config file that churns its own diff and a fixture nobody can assert on. The generator sorts before emitting, which is free semantically since JSON objects are unordered. (b) **Two new std/json cursor primitives**: `curKeyAt` and `curValueAt`. Decoding a map means walking children you cannot NAME at generation time, and the existing API could only ask for a field by key (`curField`) or an ARRAY element by index (`curChild`, which returns -1 for an object). `keys()` answered the same question for the root only. `curValueAt` is deliberately separate from `curChild` rather than relaxing that kind check — "element i of an array" and "entry i of an object" are different questions, and one function answering both would make an object walked as an array look like it worked. (c) **`std/sort` injected only when a deriving struct actually has a map field**, found syntactically in the resolver, so the common derive keeps its dependency list. Non-string keys are refused with the reason (`1` and `"1"` would round-trip to the same place), pinned in `tests/errors/deriveJsonIntKeyedMap.milo`. **Left: `[T; N]`** — an array plan is nearly the `vec` plan, but decode has to reject a wrong length rather than push, and there is no fixed-array literal to accumulate into without knowing N at generation time. |
| 14 | **`fromJson` takes an owned string, so every borrowed call site clones** | M | L | **RE-VERIFIED 2026-08-15, and it is a TRADE-OFF, not an oversight — decide before changing it.** Confirmed: `P.fromJson(text)` on a `&string` is rejected with `cannot move the borrowed value out of 'text'` and the `.clone()` hint. But the owned parameter is not arbitrary — `Json` OWNS its `source: string` and the whole cursor API indexes into it (`jsonKeyEq(self.source, …)`, `jsonMaterializeStr(self.source, …)`), so `Json.parse` genuinely needs ownership. A `&string` form therefore cannot avoid the copy, it can only MOVE it: borrowed callers stop writing `.clone()`, and owned callers — who currently hand their buffer straight in with zero copies — start paying one. Note the signature change is source-compatible either way, since a bare owned value auto-borrows; only the allocation profile moves. Three options: (a) switch to `&string` and accept the copy for owned callers; (b) emit BOTH, `fromJson(string)` and a borrowing sibling, which is the incremental-API shape but two names per deriving struct; (c) leave it — the diagnostic already names the fix. Pick one rather than discovering the regression afterwards. | `src/derive-json.ts:261`, `std/json.milo` (Json.source) |
| ~~16~~ | ~~**An unbound `??` result leaks its owned value**~~ | H | M | **CLOSED 2026-08-15.** `DefaultValue` joins `isOwnedTempExpr`, gated by `defaultValueOwnsResult`. The entry's stated blocker was wrong — it feared the right operand might be an alias, but `??` MOVES both operands (measured: the source reads back empty afterwards), so the result is always fresh. The real constraint turned out to be narrower and is what the gate encodes: codegen zeroes a moved source only when it can find its slot, which it cannot for every `Ident` (an immutable `let` is an SSA register, not an alloca), so only an owned TEMP branch or a string literal (cap 0, drop is a no-op) counts. `o ?? s.field` and `s.opt ?? d` therefore still leak rather than risk a double free — recorded here rather than fixed, since closing them means giving codegen a slot for every moved source. Probing note that cost time: a string LITERAL payload leaks nothing, so a repro must build both operands on the heap or it reads clean. Closing it also made four std fixtures leak-free that nothing had connected to `??` — `argparseAbsentVsEmpty`, `fetchResponseParse`, `httpAbsentVsEmpty`, `jsonPath`. Locked by `tests/fixtures/coalesceUnboundTempDrop.milo` on `tests/leak-clean.txt`. See also the use-after-move half, fixed the same day in `checker.ts checkDefaultValueExpr`. |
| ~~18~~ | ~~**A spawned task's closure environment leaks, and releasing it needs a task-reclaim hook**~~ | M | M | **CLOSED 2026-08-20.** Captures used to be COPIED out of the environment into private allocas, so a body that consumed one -- `runWorker(id, total, maxId, ch)` takes its handles by value -- zeroed its own copy while the env slot kept a live header aimed at memory the callee had already freed. That is why every attempt to run the glue failed, and why the two failures looked unrelated: `osThreadTrampoline` calling it SIGSEGV'd battleConcurrency on a shared ChannelHandle whose refcount went one low, and `reapTask` calling it silenced six milojs async fixtures. Same cause. The fix is that a move closure's env slot IS the capture's storage (`src/codegen.ts` genClosure aliases the slot instead of copying), so the move zeroing codegen already emits writes THROUGH to the env, and the glue skips any slot that is all zero -- the moved-from marker. `String` had its own liveness check (cap > 0); `ChannelHandle.drop` dereferenced `_ptr` to reach the refcount and read through null, which is the 0x8 fault ASan named. Both release points are now live: the detached-thread trampoline and `reapTask`. Measured 120 leaks / 7360 bytes over 20 `Promise.blocking` calls -> 0; a spawned task's captures went from ZERO destructor runs to exactly one. milojs is 330/330 and 11/11. Gated by `tests/fixtures/taskCaptureDropped.milo` (counts Drop runs, not bytes) and `moveClosureCaptureConsumed.milo` (both release paths), both in `tests/leak-clean.txt`. Diagnosed by pointing ASan at the crash rather than reasoning about it -- the report named a use-after-free of a shared handle, not the double free that had been assumed for two sessions. **NARROWED to one cause 2026-08-15, with the answer supplied by milojs.** Every OTHER closure environment is now released (the drop glue in #20/#23 shipped); a SPAWNED one is the exception. The ownership is correct — an owning closure is non-Copy so `Task.spawn(f)` moves it, and `spawnWithStack` `forget`s it after handing the pointers over — and the release is one line in `reapTask`: read the drop function from slot 0 of the environment and call it. Doing that turns milojs's async path SILENT: `await` on a bound async function prints nothing and exits 0, across six of its fixtures (doubleBind, generatorProtocol, microtaskHandlerGcRoot, objectGeneratorMethods, promises, staticAccessors). Isolated to exactly that release — with every other part of the glue in place and only it removed, all six pass. **The cause is not a bug in the glue, it is a missing contract.** milojs stores RAW TASK POINTERS and environment indices in parallel arrays that outlive the task body, and its own source already comments the hazard above `removeGen`: a stale body-task pointer collides with a future generator whose freshly-spawned task reuses the same freed address. Three holders, all in its `Interp`: `genTask`/`genEnv` (dropped only on the TERMINAL read, so `for (const x of g()) break;` retains one indefinitely — the unbounded case), `actTask` (walked every event-loop turn and compared by raw pointer), and `awaitTask`/`suspendedTask` (live until the promise settles). Any embedder keeping arrays keyed by task pointer has the same problem; milojs is just the first. **So decide the hook before building either half**: a "this task is about to be reclaimed" callback the runtime invokes before `reapTask` frees anything, so a holder drops its record first. **Four requirements from the first real consumer** (milojs, which will implement its half once the shape is fixed): (1) stated as a PROPERTY rather than a timing, which is how the consumer wants it: **no task pointer a holder retains may ever compare equal to a task it did not receive the hook for.** Freeing alone is survivable for a holder that only compares pointers and never dereferences them; REUSE is the moment a lookup silently resolves to the wrong record, and those are not the same instant; (2) it must fire for EVERY terminated task, including one abandoned without completing — `for (const x of g()) break;` leaves a generator body task that never finishes, and a completion-only hook leaves that case unbounded rather than merely windowed; (3) it must hand back the same identity the holder stored, the task pointer; (4) the handler runs while the scheduler is mid-reap, so it must not need to allocate or spawn to do its work. Note (2) is the one that constrains the design most — it means the hook belongs wherever a task is torn down, not on the completion path. **CORRECTION 2026-08-20: "every OTHER closure environment is now released" is not true of a DETACHED OS THREAD.** `osThreadTrampoline` frees the environment BLOCK and never runs its glue, so an owning capture there leaks too — measured at ~5 allocations / 336 bytes per `Promise.blocking` (each captures the worker's channel handle, whose refcount then never reaches zero); 20 promises leak 6720 bytes. Green tasks with an owning capture leak ~2 allocations each; a green task capturing nothing owned is clean. So there are two leaks with different causes: `reapTask` never frees the green environment at all, while the detached trampoline frees the block and skips the glue. **And the naive fix fails a SECOND way, independent of milojs**: calling the glue in the trampoline zeroes the leak (100 -> 0 across every shape tried) and SIGSEGVs `tests/fixtures/battleConcurrency.milo`, because a `move` closure body may CONSUME its captures (`chSend.send(result)` moves the handle out) and the glue then drops them twice. Whether a capture survives the body is per-closure, so neither trampoline can decide it — which points at the same missing contract from a second direction, and suggests the fix is move-aware glue (zero an env slot when the body moves a capture out) rather than a release hook alone. | `std/runtime.milo reapTask`, milojs `src/eval.milo` |

| ~~20~~ | ~~**A `move` closure never drops what it captured**~~ | H | M | **CLOSED 2026-08-20.** Captures used to be COPIED out of the environment into private allocas, so a body that consumed one -- `runWorker(id, total, maxId, ch)` takes its handles by value -- zeroed its own copy while the env slot kept a live header aimed at memory the callee had already freed. That is why every attempt to run the glue failed, and why the two failures looked unrelated: `osThreadTrampoline` calling it SIGSEGV'd battleConcurrency on a shared ChannelHandle whose refcount went one low, and `reapTask` calling it silenced six milojs async fixtures. Same cause. The fix is that a move closure's env slot IS the capture's storage (`src/codegen.ts` genClosure aliases the slot instead of copying), so the move zeroing codegen already emits writes THROUGH to the env, and the glue skips any slot that is all zero -- the moved-from marker. `String` had its own liveness check (cap > 0); `ChannelHandle.drop` dereferenced `_ptr` to reach the refcount and read through null, which is the 0x8 fault ASan named. Both release points are now live: the detached-thread trampoline and `reapTask`. Measured 120 leaks / 7360 bytes over 20 `Promise.blocking` calls -> 0; a spawned task's captures went from ZERO destructor runs to exactly one. milojs is 330/330 and 11/11. Gated by `tests/fixtures/taskCaptureDropped.milo` (counts Drop runs, not bytes) and `moveClosureCaptureConsumed.milo` (both release paths), both in `tests/leak-clean.txt`. Diagnosed by pointing ASan at the crash rather than reasoning about it -- the report named a use-after-free of a shared handle, not the double free that had been assumed for two sessions. **RE-VERIFIED 2026-08-16 WITH DESTRUCTOR COUNTS, and the entry is now HALF closed — the split is clean.** Counting `Drop` runs instead of leaked bytes makes the boundary exact (a byte count cannot tell "never destroyed" from "destroyed and the allocator kept the page"): a `Res` with an `impl Drop` moved into a `move` closure is destroyed exactly ONCE whether the closure is called or never called, so the plain-closure half of this entry is fixed by the owning-`fn` type and the env drop glue landed 2026-08-16. The same resource moved into `Task.spawn(move () => …)` and then `join()`ed is destroyed **ZERO** times. So what is left is not closure lowering at all — it is the task-reclaim hook of #18/#23: the scheduler owns the env after spawn and there is no point at which anything runs drop glue over its slots. Do not re-attempt the closure side; attempting the reclaim side blind is how a use-after-free got introduced and reverted on 2026-08-16 (`reapTask` freeing the env while the `fn` value was still Copy). Minimal repro is three lines and needs no leak tooling: give any struct an `impl Drop` that prints, move it into a spawned task, join, and count the prints. **FOUND 2026-08-15 while closing #18, and it is the larger half of it.** Freeing the env BOX (which #18 did) does not free the owned values moved into it. `var s = "heap"; s.pushStr(...); let f = move(): void => print(s); call(f)` leaks 64 bytes with no task and no spawn involved — the leak is in closure lowering, not the scheduler. Spawning makes it worse only by adding the box: the same shape through `Task.spawn` capturing a heap `string` plus a `Vec<i64>` leaks 2 blocks / 144 bytes. There is no per-closure env destructor at all today: codegen emits `malloc` for the env and stores captures into it, and nothing anywhere runs drop glue over those slots. **ROOT CAUSE FOUND 2026-08-15, and it is worse than a leak: a `move` closure OWNS heap memory and its value is `Copy`.** `src/types.ts` isCopy returns true for `fn`, so `let g = f; call(f); call(g); call(f)` compiles and runs with three live copies of one heap env — measured. Nothing frees it, so today the only symptom is the leak. But it means **no destructor can be attached to a closure value at all**: N copies would each drop the same env. That is an owning value that duplicates freely, which is the one thing single-ownership is supposed to exclude, and it was proven by trying — freeing the env in `reapTask` produced a real use-after-free (see #18). Refcounting the env is the other way out and Milo has rejected RC. So the ordering is forced: **owning closures must become non-Copy BEFORE any drop glue exists**, and that is a type-system question, not a codegen one. `fn` is one type constructor covering three runtime shapes — a bare function pointer (genuinely Copy), a by-reference closure (stack env, nothing to own), and a `move` closure (heap env, owns its captures) — and a `() => void` parameter accepts all three, so no per-value rule can be enforced at a signature. Options, none free: (a) make ALL `fn` values non-Copy — sound, rejects `let g = f` on plain function pointers, which is a real regression; (b) split the type constructor the way Rust splits `fn` pointers from `Fn` closures, so a signature says which it takes — principled, and the change lands in every signature that accepts a callback; (c) keep leaking and document it. **SIZED — this is a closure-ABI change, not a one-function fix, which is why it is filed and not done.** A closure value is `{ ptr fn, ptr env }`, and NOTHING in that pair says how to release the env: a non-capturing closure's env is `null`, a by-reference closure's is a STACK alloca holding pointers to the originals (free it and you corrupt the frame), and a `move` closure's is `malloc`'d and owns by-value copies. Codegen knows which at the creation site — but `needsDropCg` decides by TYPE, and two closures with the identical `() => void` type can be any of the three, so drop-by-type cannot tell them apart. It also has to survive a function boundary: `Task.spawn(f)` takes the closure BY VALUE, so the frame that must eventually release the env is not the frame that built it and has no access to the shape. That leaves one honest design — widen the pair to `{ fn, env, drop }` and have every creation site fill in per-shape glue (the capture list is known at lowering) — which touches every closure call site, the `Task` struct and `reapTask` in `std/runtime`, the `_callClosureVoid` builtin, and emit-js. Registering a drop against the local at the creation site is NOT a shortcut: it fixes `let f = move(…)` and leaves every closure that is passed onward still leaking, which is the shape that motivated the entry. Two hazards: a closure may be called many times (drop is on the VALUE's death, not the call), and a by-reference capture must NOT be dropped — only `move` captures are owned. Until this lands, `tests/leak-clean.txt` cannot carry any fixture whose closure captures owned heap, so the leak gate stays blind to exactly the shape most programs write. | `src/codegen.ts` (closure lowering), `std/runtime.milo reapTask`, `tests/leak-clean.txt` |

| 21 | **milo-self has no `cannot infer Vec element type` diagnostic, and the soundness ratchet only just noticed** | M | **CLOSED 2026-08-16.** `milo-self` already inferred a Vec's element from the first `push` — what it lacked was the failure case: an unannotated `Vec.new()` that never gets one returned `unknown` quietly, so the fixture compiled clean and the ratchet counted it as UNSOUND. Ported the oracle's deferred machinery: `pendingVecInfer` on the Checker records each unannotated `Vec.new()`/`Vec.withCapacity()` binding, `setVarType` clears it (refining the local IS the resolution a push performs), and anything still pending when `checkProgram` ends is reported with the oracle's own wording. Verified both directions — the fixture is rejected, `var v = Vec.new(); v.push(3)` still runs — and verified it does not over-reject: `milo-self check src-milo/main.milo` and the five std files that use an unannotated `Vec.new()` (sha256, sha1, deflate x2, zip) are all clean, so stage 2 keeps building. **This was the ONLY entry in the soundness ratchet's 118-row manifest that had regressed**, so the ratchet now passes; it had been skipped for weeks because the fixpoint failed ahead of it and never ran. M | **DIAGNOSED 2026-08-15 — this is NOT a compiler regression, and bisecting it as one is a dead end (I did).** `bun scripts/selfhost-rejects.ts --check` fails on `errors/vec_infer_no_push`, and CI's Self-host job with it. The cause is `f608cdb0`, which taught `tests/annotations.ts parseExpectedError` to see a `@error:` annotation that TRAILS the offending statement. Ten fixtures write it that way; until then the parser returned null for them, the harness's `if (want && !out.includes(want))` check was skipped entirely, and the verdict fell through to `ok` — "milo-self rejected this for SOME reason" was the whole assertion. Nine of the ten survive the newly-enforced check. This one does not: for `var xs = Vec.new()` never pushed to, the oracle says `cannot infer Vec element type — no 'push' found to infer from`, while milo-self says `cannot access field 'len' on type <unknown>` twice. Verified by hand against milo-self binaries built at commits the ratchet called OK — the SAME wrong message — so the behaviour is unchanged and only the assertion is new. So: the program is still REJECTED (no soundness gap, no over-acceptance), the diagnostic is just generic. src-milo infers the element type from the first push in codegen (`inferVecPushHints`), not in the checker, so there is no place that currently knows the annotation is missing. The manifest header says entries are never removed, and this one only ever qualified because of the parser bug — so decide explicitly: implement the diagnostic in src-milo's checker (needs the fixpoint + sweep gates), or correct the manifest with the reason written down. Leaving CI red is the one option that is not a decision. | `src-milo/checker/`, `tests/selfhost-rejects-manifest.txt`, `tests/annotations.ts`, `scripts/selfhost-rejects.ts` |

| 22 | **Diagnostics have no stable codes, so machine consumers classify on message text** | M | **RE-VERIFIED 2026-08-16 through the JSON surface, and the split is sharper than "no codes".** WARNINGS already carry one, because `--deny=<name>` needs a name to key on: `milo check --json` emits `"code": "index-clone"` beside the message. ERRORS carry none at all, so the same JSON payload is machine-classifiable for one severity and text-matched for the other. **Scale, measured rather than estimated:** 552 `this.error(...)` sites in `checker.ts` plus 29 in `parser.ts`. That is the size of a naming job, not a plumbing job — the field already exists on `Diagnostic` and the JSON already serialises it when set, so nothing needs building; 581 messages need names, and the naming scheme is a design decision (flat kebab like the warnings, or a numbered E-series). **Partly mitigated already, which is why this is not urgent:** 258 fixtures in `tests/errors/` pin an error's message text, so a message cannot drift silently even without a code — the gap is machine CLASSIFICATION by an external tool, not drift. A cheap first slice, if wanted, is coding only the errors an editor or LSP client actually branches on rather than all 581. M | **FOUND 2026-08-15 shipping `milo check --json` ([docs/json-api.md](json-api.md)).** The payload has a `code` field and almost nothing fills it: `src/checker.ts` has ~543 `this.error(` call sites and 2 diagnostics carry a code. Every consumer therefore classifies on the rendered message — `scripts/fuzz-check.ts` already greps message text, and a reworded message silently changes what it counts. It also blocks the obvious uses of the JSON surface: a CI annotation that suppresses one rule, a ratchet on "no new errors of kind X", an editor quick-fix keyed to a diagnostic. The work is a scheme plus 543 mechanical edits, and the scheme is the hard half: a per-SITE code is unstable (moving an `if` renumbers it) and a per-MESSAGE code is unstable (rewording it changes it), so codes have to name the RULE — `E-MOVE-USE-AFTER`, `E-BORROW-ALIAS-MUT` — which means grouping the call sites by rule first, and that grouping does not exist anywhere today. `docs/errors.md` (generated from the 244 error fixtures) is the closest thing to a rule inventory and is the place to start: each distinct message there is roughly one rule, so the catalog can seed the scheme and then gate it (every code appears in a fixture, every fixture's diagnostic has a code). Do NOT half-land it: a surface where 10% of diagnostics have codes is worse than none, because a consumer cannot tell "uncoded" from "different rule". | `src/checker.ts`, `src/diagnostics.ts`, `docs/errors.md`, `scripts/gen-error-catalog.ts` |

| ~~22~~ | ~~**Closure type split — the drop glue that it unblocks**~~ | H | M | **CLOSED 2026-08-20.** Captures used to be COPIED out of the environment into private allocas, so a body that consumed one -- `runWorker(id, total, maxId, ch)` takes its handles by value -- zeroed its own copy while the env slot kept a live header aimed at memory the callee had already freed. That is why every attempt to run the glue failed, and why the two failures looked unrelated: `osThreadTrampoline` calling it SIGSEGV'd battleConcurrency on a shared ChannelHandle whose refcount went one low, and `reapTask` calling it silenced six milojs async fixtures. Same cause. The fix is that a move closure's env slot IS the capture's storage (`src/codegen.ts` genClosure aliases the slot instead of copying), so the move zeroing codegen already emits writes THROUGH to the env, and the glue skips any slot that is all zero -- the moved-from marker. `String` had its own liveness check (cap > 0); `ChannelHandle.drop` dereferenced `_ptr` to reach the refcount and read through null, which is the 0x8 fault ASan named. Both release points are now live: the detached-thread trampoline and `reapTask`. Measured 120 leaks / 7360 bytes over 20 `Promise.blocking` calls -> 0; a spawned task's captures went from ZERO destructor runs to exactly one. milojs is 330/330 and 11/11. Gated by `tests/fixtures/taskCaptureDropped.milo` (counts Drop runs, not bytes) and `moveClosureCaptureConsumed.milo` (both release paths), both in `tests/leak-clean.txt`. Diagnosed by pointing ASan at the crash rather than reasoning about it -- the report named a use-after-free of a shared handle, not the double free that had been assumed for two sessions. **DECIDED and the TYPE HALF SHIPPED 2026-08-15: `move (T) => R` is a distinct, non-Copy type.** Option (b) from the measurement below was chosen. A closure literal is typed owning only when it is `move` AND actually captured something — `move` with no captures lowers to a null environment and owns nothing, so restricting it would buy nothing. `owning` is deliberately NOT part of `typeEq`: a non-owning value (bare function, by-reference closure) passes to a `move` parameter freely, because transferring something that owns nothing loses nothing. **The corpus needed no changes at all** — 868/868 fixtures, examples, LSP, src-milo, all green with owning closures non-Copy, which is what the measurement predicted. std/runtime's four ownership-taking signatures (`Task.spawn`, `spawnWithStack`, `spawnOsThreadDetached`, `Promise.blocking`) now spell it. Formatter taught both positions apart (`move (T) => R` the type keeps its space, `move(x: i64) => x + 1` the expression does not) and the use-after-move hint no longer suggests a `.clone()` closures do not have. **LEFT — and this is now the whole of #18 and #20**: the drop glue. An owning closure is non-Copy, so single ownership holds and a destructor is finally SOUND to attach; nothing attaches one yet, so captures still leak. Needs a per-shape env drop function, called where the owner dies — scope end for a local, the callee for a by-value parameter, `reapTask` for a spawned task — and the enforcement rule that an owning value may NOT pass to a non-owning parameter (harmless today because nothing frees, required the moment something does). | `src/types.ts`, `src/checker.ts`, `src/parser.ts`, `std/runtime.milo` |

| 23 | **The closure drop glue: `forget` SHIPPED, the environment header is next** | H | M | **`forget(x)` landed 2026-08-15 — the prerequisite is done.** It consumes its argument like any other transfer (reading it after is the usual use-after-move error) and simply does not run the drop: for a named place that is the same bookkeeping every transfer does — zero the slot, clear the alive flag — so scope exit walks past it. Not `unsafe`, because leaking is memory-safe; it is merely usually wrong. Verified by leaking on purpose: 50 forgotten buffers report 50 leaks, the identical program without it reports 0. **Testing note worth keeping**: the ONE-value version of that test reported clean while leaking, because a single forgotten pointer can still sit in a stack slot at exit and conservative leak scanning reads it as reachable — a leak test with one allocation proves nothing. `codegen-js` treats it as a no-op (JS is garbage collected) and its fixture is deliberately OFF `tests/leak-clean.txt`. **LEFT, unchanged and now unblocked**: give every closure environment a leading `ptr` header holding a per-shape drop function (null for a by-reference closure, whose environment is a stack slot), shift the capture GEPs by one, and have `emitDropValue` for an owning `fn` load and call it — which keeps a closure at TWO words, so no call site, `Task` field or emit-js signature changes. Then `spawnWithStack` `forget`s its parameter after memcpying the pointers out, `reapTask` calls the header, and an owning value may no longer pass to a non-owning parameter. | `src/codegen.ts genClosure`, `std/runtime.milo` |

| 24 | **`unnecessary-pub`: a `pub` decl that no other file references** | M | L | **FILED 2026-08-15 from a review of [astral-sh/hawk](https://github.com/astral-sh/hawk), a workspace-aware Cargo lint that answers exactly this question for Rust.** `src/visibility.ts` already computes both halves of the answer and throws one half away: it walks every top-level reference to decide *"is this reference in the declaring file?"* (that walk is the private-access error), and it holds `declOrigins`, mapping each name to the files that define it. Invert the same walk: a `pub` decl whose referencing files are all its own defining file never needed `pub`. No new analysis and no new data structure, one more accumulator in a pass that already runs. The fix is machine-applicable (delete the token). Ship it `offByDefault: true` alongside `unused-import` in `src/warnings.ts`, because `std/` will light up until it is triaged. Payoff is concentrated there and in `src-milo/`: 801 `pub fn` + 422 `pub extern` + 159 `pub struct` in `std/`, 813 `pub` in `src-milo/`, and the flat namespace makes every shipped name permanent ([breaking-changes.md](breaking-changes.md) is the only migration path users get). **Sequencing caveat:** if Tier 2 #1b adds a package-scoped level, this lint's answer becomes three-valued (file-private / package / public) instead of two. The walk is unchanged; the suggestion it prints is not. | `src/visibility.ts`, `src/warnings.ts`, Tier 2 #1b |

| 25 | **Warnings can be allowed but never *expected*, so a stale suppression is invisible** | M | **DONE 2026-08-16.** `--expect=<warning>` suppresses the finding like `--allow` and reports when it stops occurring, as `unfulfilled-expectation` (on by default, and itself deniable so a project can make a stale suppression fail the build). Two things made it more than a rename of `--allow`, both found by testing rather than by design: an off-by-default lint must be ENABLED by expecting it, or it never runs, never fires, and reports itself unfulfilled against code that does contain the finding; and the flag family accepted misspelled names in silence, so `--deny=indx-clone` looked like a project enforcing a lint and enforced nothing. Unknown names are now refused with a did-you-mean, which closes the complaint `src/warnings.ts`'s own header opens with. Not done: hawk's REQUIRED reason string on every suppression, which is the other half of turning the list into a written record. Locked by `tests/expectWarning.test.ts`. L | **FILED 2026-08-15, same hawk review.** `--allow=` / `--deny=` / `--deny-all` (`src/warnings.ts`, `src/cli-help.ts`) already match Clippy's and hawk's `-A`/`-W`/`-D` one for one. The level neither of ours has is hawk's **`expect`**: suppress the finding *and* report when the finding stops occurring. An `allow` that outlives its cause is silent forever and nothing ever deletes it; an `expect` deletes itself the moment the code it excused is fixed. Hawk pairs it with a required `reason` on every suppression, which turns the suppression list into a written record of accepted debt rather than a pile of undated opt-outs. Cost is one more level through the existing config plumbing plus a post-check pass comparing expected-vs-fired. **Do this before #24 and Tier 2 #20, not after:** those two are the lints that will generate a large one-time suppression list, and that list is exactly the thing you want self-cleaning. | `src/warnings.ts`, `src/checker.ts` (level plumbing near the `unused-move`/`unused-import` defaults), `src/cli-help.ts` |

| 24 | **An enum payload cannot be mutated in place, even through `&mut`** | H | **RE-VERIFIED 2026-08-16, unchanged, and the diagnostic actively misleads.** `fn bump(n: &mut Node)` matching `Node.Leaf(v) => { v = v + 1 }` is rejected with `cannot assign to immutable variable 'v'` — correct as far as the rule goes — but the hint reads *"declare with 'var' instead of 'let' to make it mutable"*, and there is no `let` here to change: `v` is a MATCH BINDING, and no spelling of the pattern makes it mutable. The reader is sent to fix something that does not exist. That hint is generic assignment-to-immutable advice reaching a place where it cannot apply, so **The cheap half is DONE 2026-08-16**: a pattern binding is now marked as such on its `VarInfo`, and assigning to one says so and names the move that works (rebuild the variant: `n = Node.Leaf(v + 1)`), which is O(1) and clones nothing. Locked by `tests/errors/matchBindingAssign.milo`; the ordinary `let` case keeps the old advice. The expensive half — binding a payload as `&mut` so it can be mutated in place — is unchanged and is the real entry. M | **VERIFIED 2026-08-15 (user-reported).** Matching on a `&mut Enum` binds payloads immutably, so the natural spelling is rejected: `match n { Node.Leaf(v) => { v = v + 1 } }` on `n: &mut Node` gives `cannot assign to immutable variable 'v'` — with a hint that makes it worse, `declare with 'var' instead of 'let'`, which cannot be done to a pattern binding. This lands on anything with a tree: an AST rewrite, an interpreter's value cells, a state machine — exactly the programs Milo is being pointed at. **There IS an O(1) workaround and it costs no clone** (verified, including a heap `string` payload moving through untouched): take the whole enum out with `replace`, rebuild the variant, assign it back — `let old = replace(n, Node.Leaf(0)); match old { Node.Pair(s, k) => { n = Node.Pair(s, k + 1) } }`. `replace` is move-out/move-in, so this is a couple of stores, not a deep copy. It is still four lines and a sentinel value for what should be `v = v + 1`, and the sentinel has to be a variant you can name. Fixing it properly means pattern bindings inheriting the scrutinee's mutability — `&mut` scrutinee gives `&mut` bindings — which is what Rust does and is a checker change, not a representation one. The misleading hint should go regardless of when the rest lands. | `src/checker.ts` (match binding mutability), `docs/language-reference.md` |
| 25 | **A `&mut` parameter that is only written reads as unused** | L | L | **FOUND 2026-08-15 while checking #24.** `fn bump(n: &mut E)` whose body is `let old = replace(n, E.A(0))` then `n = E.A(...)` warns `unused variable 'n'` and suggests `_n` — but `n` is BOTH read (replace moves the old value out of it) and written. `VarInfo.read` is not set by a `replace` place argument nor by an assignment target, so a parameter whose entire purpose is to be written through looks dead. The suggested fix, renaming to `_n`, would then break the code that uses it. Noise on exactly the shape #24 forces people to write. | `src/checker.ts` (unused-variable, VarInfo.read) |
| 26 | **The string API mixes byte and code-point semantics with no way to tell which** | H | M | **MEASURED 2026-08-15, both halves; found by looking for the regex bug's CLASS rather than its instance.** Milo strings are UTF-8 by definition, and the API is inconsistent about whether an operation respects that. Measured by comparison, not by eye (terminal rendering hides it): `"aéb".reverse()` is correct and `"日本語".reverse()` is correct — someone did that work — while `charAt(1)` returns ONE BYTE (half of the é, not valid UTF-8), `substr(0, 2)` splits the character, and `padStart(3)` pads to three BYTES so a multibyte string comes out narrower than asked. `toUpper`/`toLower` are ASCII-only (`"aéb"` -> `"AéB"`). A reader cannot infer the rule from any of it, and the silent output is INVALID UTF-8 rather than an error — Rust, whose `String` this is modelled on, panics on a non-boundary slice instead. In `std/unicode` (found by the milojs session): `isAlphaStr` walks bytes calling `asciiIsAlpha`, so `"café"`, `"привет"` and `"αβγ"` are all false, in a module whose header promised "case conversion" it does not contain; `isNumeric` has the same shape. Its code-point layer is fine — `codepointCount("café")` is 4 — so it is specifically classification that is ASCII. **Documented 2026-08-15** at every surface that reads it: `src/builtin-members.ts` notes (which feed the editor, `milo api` and did-you-mean at once), each std/unicode definition, and that module's header. **LEFT is the semantic decision, which is the real entry**: (a) make a non-boundary `charAt`/`substr` an ERROR the way Rust does, so invalid UTF-8 cannot be produced silently — the honest fix, and it breaks code that indexes by byte on purpose; (b) add code-point-aware siblings and leave these byte-level; (c) leave it and rely on the notes. Real Unicode case mapping and a general-category `isAlphaStr` both need a GENERATED table (ß uppercases to SS, so it is not a code-point delta) — the milojs session has a generator that asks node's ICU for all 0x110000 code points and emits Milo source, verified exhaustively against node, and has offered it. Taking that is a decision about carrying ~3k lines of generated table in std, not a coding task. | `src/builtin-members.ts`, `std/unicode.milo`, `std/string` builtins |

| 27 | **A `requires` is the only guard at an FFI boundary, and it is off at -O2** | H | M | **FOUND 2026-08-16 by grepping for comments that ARGUE rather than describe, then following the contract.** `std/crypto`'s AES-GCM entry points state their preconditions properly (`requires key.len == 32`) and pass 32/12/16 to the C API as CONSTANTS regardless of what arrived. Contracts are asserted in `--debug` and DROPPED at the default `-O2`, so an ordinary build encrypted happily with an 8-byte key — reading 32 bytes from an 8-byte buffer. An out-of-bounds read reachable from safe Milo through a `pub` std API, plus a silent crypto failure where the caller believes their key was used. Fixed by checking the lengths at runtime as well (2026-08-16, `aesGcmLengthGuards` fixture). **The general rule, which is what makes this an entry rather than a bug report**: a `requires` on a value that crosses into C is categorically different from one on a Milo-side index. Milo bounds-checks its own indexing by default, so a violated `requires h.count.len >= 16` in `std/inflate` traps; nothing checks a pointer handed to `CCCryptorGCMOneshotEncrypt` with a constant length. At an FFI boundary the contract is the LAST line of defence, and it is exactly the line that `-O2` removes. **That promotion was BUILT and REVERTED 2026-08-16 — it is the wrong fix, and the reasons are worth keeping.** Keeping `requires` asserted at any -O for an unsafe-bodied fn works (10 fns across std and src-milo qualify: the 8 crypto entry points plus `bumpNew`/`poolNew`, all cold, so the cost is nil) and it does close the hole structurally. Two things kill it. (1) **A violated contract ABORTS, and these lengths are not all the program's**: `aesGcmDecrypt`'s `tag` and `iv` come off the wire, so a peer sending a 5-byte tag would take the process down — turning a memory bug into a remote denial of service. (2) It changes what `--no-contract-checks` and `-O2` PROMISE, which `tests/contractChecks.test.ts` pins as a documented guarantee; that is a language decision, not a std fix. **What shipped instead layers correctly**: `--debug` aborts on the contract (a developer sees the bug at once), `-O2` drops it and the explicit check returns `Result.Err` (recoverable, no abort on peer input), and the contracts stay for the PROVER — removing them broke a real static proof in `examples/net/termpair/encryption.milo`, which is how I learned they were carrying more than documentation. **A SECOND instance, found by auditing the other nine candidates rather than stopping at crypto (2026-08-16): `std/pool`'s `poolNew`.** It threads an 8-byte free-list pointer through each block, so `requires size >= 8` is the only thing standing between a caller and a write past the allocation — and at `-O2` there is nothing. `Pool.new(2, 64)` in an ordinary build: ASan reports `heap-buffer-overflow WRITE of size 8`. Same shape as the AES-GCM one, a category worse, because that was a read. Guarded the same way (`poolBlockSizeGuard` fixture). Of the ten `unsafe`-plus-`requires` fns, `bumpNew` is the one that is genuinely fine — `alloc` bounds-checks against `cap`, so a zero capacity yields an arena that refuses to allocate rather than one that hands out pointers (verified under ASan). So the pattern is not universal, and a blanket rule would have been wrong about a third of it. **The open question, now with two data points instead of one**: should a lint flag an `unsafe`-bodied fn whose `requires` has no matching runtime check? It would have caught both, and the census says the candidate set is ten fns, so it is unlikely to be noisy. The hard part is recognising that a check IS present — `requires size >= 8` against `if size < 8 { return Err }` needs the negation matched, not the text. Worth building only if the answer to "is a hand-rolled check beside every FFI contract the intended pattern" is yes; if the answer is instead that contracts should be enforceable at an FFI boundary by some other means, the lint is the wrong shape. That decision is still open. | `std/crypto.*.milo`, `src/checker.ts` (contract lowering), `docs/language-reference.md` (contract-checks) |
| 28 | **Destruction order is unspecified, and the two places that have one disagree** | M | L-M | **MEASURED 2026-08-16 with destructor counts.** Nothing in `docs/language-reference.md` says what order values are destroyed in — the Drop section covers when a destructor runs, never in what sequence — so the implementation simply has an order and nobody wrote it down. The two places that have one do not match: **locals destroy in DECLARATION order** (`var a = Res{1}; var b = Res{2}; var c = Res{3}` prints `drop 1, drop 2, drop 3`) while **struct fields destroy in REVERSE** (`Pair { x: Res{1}, y: Res{2} }` prints `drop 2, drop 1`). Vec elements follow index order. Reverse-of-construction is the universal RAII convention (Rust, C++) and it is what the fields already do; locals are the outlier, and they are the case where order is most visible because a later local is the one most likely to depend on an earlier one. Milo's exposure is genuinely smaller than Rust's — a local cannot hold a reference to another local, since references are second-class — so this is not a use-after-free waiting to happen; it is an OBSERVABLE side-effect order (a flush before a close, a log line, an unlock) that no document promises and that two constructs answer differently. **Changing local order is a semantic change to existing programs, so it is a language decision rather than a bug fix** — hence filed rather than done. Whichever way it goes, the answer belongs in the language reference; today a reader cannot find out except by running it. **Related, same measurement session: a `Drop` global is NEVER destroyed** — `var G: Res = Res{9}` runs no destructor at program exit. Defensible (Rust does not run destructors for statics either, and the OS reclaims at exit), but likewise undocumented, and it means a global holding a flush-on-drop buffer silently loses its tail. | `src/codegen.ts` (scope-exit drop emission, `ensureStructDropHelper` field loop), `docs/language-reference.md` (Drop section) |
| 29 | **A stored closure can only be called when it happens to be a struct field** | M | L-M | **MEASURED 2026-08-16 while probing closure escape.** `fn` values are first-class enough to put in a `Vec`, a fixed array, a struct, and to return from a function — but calling one only parses in a single position. `b.f()` works, because it parses as a METHOD call and never as "call the value this expression produces". Every other position is a parse error at the call parens: `v[0]()` (Vec element), `a[0]()` (array element), and `mk()()` (a returned closure) all report `unexpected token ')'`. The workaround is a binding — `let f = v[0]` then `f()` — which costs nothing at runtime and works everywhere, so this is expressiveness rather than a hazard. **Why it is worth an entry rather than a shrug**: the type system already lets a program build a `Vec<Thunk>` and hand it around, the escape rules already reason correctly about closures stored in containers (verified the same session: storing a by-reference closure in a struct, a Vec, a global, returning one, and passing one to a retaining fn are all correctly rejected), and codegen already calls closures through `ClosureCall`. It is the surface syntax alone that stops at `postfix-call only after an identifier or a field`, so a dispatch table — the reason to keep closures in a Vec at all — reads worse than it needs to. Grammar question, not a semantics one: allow a call suffix after any postfix expression. | `src/parser.ts` (postfix/call parsing), `docs/grammar.ebnf` |

| ~~30~~ | ~~**`@noCopy` and `Drop` types are silently duplicated by `v[i]`, so a handle is released twice with no error**~~ **CLOSED 2026-08-22.** Taking a `Drop` or `@noCopy` element out of a container by index is now an error, checked above the allow-gate because `--allow=index-clone` silences a cost and not a double release. The diagnostic redirects to the three spellings that stay legal: borrow it (`for x in v`), read one field (`v[i].n`), or take it out for real (`v.remove(i)` / `v.pop()`). Locked by `tests/errors/indexDropElement.milo`, `tests/errors/indexNoCopyElement.milo` and `tests/fixtures/indexResourceAllowed.milo`. Swept for over-rejection before landing: every example builds and runs, and all 7 sibling package suites pass, so nothing was relying on the old behaviour. | H | L-M | **FOUND 2026-08-17 while testing whether `.clone()` could serve as an `index-clone` escape hatch; the answer turned into a safety finding.** Taking an element out of a container by index copies it memberwise, and that path consults neither `@noCopy` nor a `Drop` impl, so both mechanisms that exist to stop a handle being duplicated are bypassed by one spelling. **`@noCopy`:** `@noCopy struct Texture { id: u32 }` with a consuming `fn free(self: Self)`, one element pushed into a `Vec<Texture>`, then `let a = v[0]` and `let b = v[0]`, prints `glDeleteTextures 7` twice, with an `index-clone` warning and no error. `docs/language-reference.md` ("`@noCopy` — move-tracked handles") states the opposite in as many words: "releasing twice, or using after release, is a compile error". Move checking never engages because the copy was never modelled as a move. **`Drop`:** `struct Fd { fd: i32 }` with `impl Drop for Fd` printing `close(self.fd)`, one element pushed, `let a = v[0]`, prints `close(3)` twice (the copy and the container's own element). Not a synthetic shape: the lint fires on real `std/io` `File`, whose `Drop` closes the descriptor, and `std/http` `Socket` and `std/fetch` `TlsStream` are the same shape, so `Vec<File>` plus `let f = v[0]` is a double close of a live fd in safe code with zero `unsafe`. Rust makes `Copy + Drop` impossible for exactly this reason and forces an explicit clone; Milo has no such rule on the index path, and the memberwise copy of an `i32` fd or a `u32` texture name is a duplicate of the resource, not a copy of a value. **Fix, which is also the answer #7 asks for on this slice:** make index-out-of-container an ERROR when the element type has a `Drop` impl or carries `@noCopy`. That converges the index spelling with the field spelling, which already errors (`cannot move 'b.t' out of the borrowed 'b'`), rather than adding a fourth answer, and it leaves the general `v[i]` clone alone: these two categories are precisely the ones where a memberwise copy has no correct meaning. Blocked on nothing; #31 only affects what the diagnostic can suggest as the fix. | `checker.ts lintIndexClone` / move checking, `codegen.ts indexAccessClones`, `docs/language-reference.md` (@noCopy), `std/io.milo`, `std/http.milo`, `std/fetch.milo` |

| 31 | **`Clone` is not derivable, so the `.clone()` two diagnostics prescribe does not exist for most types** | M-H | M | **FOUND 2026-08-17.** There is no built-in `Clone` derive and no `derive Clone { … }` template anywhere in `std`: `@derive(Clone)` on a plain two-string struct fails with `cannot derive 'Clone' — no built-in derive and no 'derive Clone { … }' template is in scope`. `.clone()` therefore exists only on builtins (`string`, `Vec`, …) and on types that hand-write `fn clone`. Two diagnostics prescribe it anyway. The field move-out-of-borrow error hints `'b' is a reference — call .clone() to take an owned copy`, and `.clone()` is the only way to say "the copy was intended" at an `index-clone` site, since the hint's free spellings (`for x in v`, `v[i].n`) do not apply when the copy is genuinely wanted. For a struct with no hand-written clone, which is most of them (including `std/http` `Route` and `std/process` `EnvChange`, two of the nine `index-clone` sites in `std`), that advice does not compile. Where a hand-written `fn clone` DOES exist the two operations are not the same operation: the implicit copy bypasses the user's impl entirely, at every nesting level (a `Vec<Outer>` whose `Outer.t: Tag` and whose `Tag.clone` prints, indexed by value, prints nothing), so rewriting `v[i]` as `v[i].clone()` is a behaviour change and not an annotation. **Pick one:** make `Clone` derivable, after which `.clone()` is the uniform "I meant it" spelling both diagnostics can name; or route the implicit copy through a user's `clone` when one exists, after which the two spellings agree by construction. Until one lands, the only `index-clone` escape is the project-wide `--allow=index-clone`, and there is no in-source suppression syntax (statement-level attributes do not parse; `@!allow(...)` file-level would need `parseStmt` work plus mirrored `src-milo/parser.milo` work, since annotating `std/` trips the self-host gate). **Ergonomics are not the blocker**: a method call on an index is already free, verified by diffing `emit-ir`, where `let a = v[0].clone()` on a `Vec<string>` is byte-identical to `let a = v[0]` apart from the embedded filename, because the receiver is a place and the clone IS the copy indexing was going to do anyway. **`index-clone` census over all of `std`** (synthetic entry importing every module, `check --json` filtered on the code): 9 sites, `fs.walkDir`, `fs.rmdirAll`, `http.handle`, `http.matchRoute` x2, `process.buildEnvp`, `process.buildCArray`, `argparse.parseFrom`, `pty.darwin spawn`. Several look intentional (both `fs` sites push to the same Vec inside the loop, so a borrow would dangle on realloc) but none can say so in source today. | `checker.ts` (derive templates, field move hint, `lintIndexClone`), `std/http.milo`, `std/process.milo` |
| 32 | **A call-once closure passed to a function can still be called twice there, and the second call silently returns a wrong answer** | M-H | H | Moving a capture out of a `move` closure empties the environment slot the capture lives in, so the closure can only run once. Where the closure is a LOCAL that is now enforced: calling it consumes it (`tests/errors/moveClosureCalledTwice.milo`). Across a function boundary it is not, because once-ness lives on the binding and not in the type: `fn twice(f: move () => i64): i64 { return f() + f() }` compiles, and `twice(move () => consume(s))` returns 52 rather than 104, since the second call reads the slot the first one zeroed. Memory-safe (verified under ASan) but wrong, and wrong quietly, which is the worst shape. The honest fix is Rust's split: once-ness belongs in the fn type, so a signature says whether it may call its argument more than once, and `move (T) => R` divides into a call-many and a call-once form. That is a type-system change touching every signature that takes a callback, which is why this is filed rather than done. A conservative alternative -- treat every call of an owning closure parameter as consuming it -- would reject the many `move` closures that consume nothing and are legitimately called in a loop, so it is not a cheap substitute. Until it lands, a body that moves a capture out is safe to write only for a closure called exactly once at a site the checker can see. | `src/checker.ts`, `src/types.ts` |
| 33 | **src-milo emits no closure-environment drop glue, so a milo-self-compiled program leaks every captured value on spawn** | M | M | Slot 0 of a closure environment is its drop glue. The reference compiler emits one; src-milo emits NULL there, so `std/runtime` falls back to `free()`ing the block and the captures inside it are never released. That is the behaviour src-milo always had, and it is a leak rather than a crash, but it is now the only reason the two compilers differ on this path. **The null header itself is not optional and must not be removed as dead weight:** `std/runtime.milo` is shared by both compilers and reads slot 0 to decide whether to call glue or free the block. src-milo had no such slot until 2026-08-20, so the runtime read the first CAPTURE and called it as a function pointer, and every milo-self-compiled program that spawned a task or a detached thread died with no output. The fixpoint stayed green throughout, because compiling the compiler never spawns anything; only the fixture sweep sees this class, which is why the sweep is now runnable from `workflow_dispatch` instead of only on a schedule. Closing this means teaching `src-milo/codegen/stmt.milo` to emit a per-closure `@__closure_N_envdrop` and the per-capture liveness flags the reference compiler uses (see `genClosure`/`emitClosureEnvDrop` in `src/codegen.ts`), at which point milo-self stops leaking here too. | `src-milo/codegen/stmt.milo`, `std/runtime.milo` |
| 35 | **An enum with a raw pointer payload is still Copy** | M | L | **FILED 2026-09-19 by the soundness sweep's WP4.** A struct with a `*T` field is now move-tracked unless `@copy`, but the rule reaches enums only through a struct field: `enum Slot { Some(*u8), Empty }` is Copy by the all-variants-Copy rule, so `let s = Slot.Some(p); take(s); take(s)` compiles and an owning handle wrapped in an enum is duplicable again. Repro: the three lines above with `fn take(s: Slot)`. Fix is the same shape as the struct rule in `isAllCopyEnum` (a pointer payload makes the enum move-tracked; `@copy` on the enum opts out, so `AttrTarget` grows `enum`), plus the `whyNotCopy` clause. Left out of WP4 because no enum in std or examples carries a pointer payload today, and the attribute's target list was scoped to structs. | this file |
| 34 | **milo-self double-frees a fixed array of owning elements passed by value** | M | M | `fn total(a: [Vec<i64>; 2])` called as `total([p, q])`: the callee drops the array's Vecs at its scope end and the caller drops them again, so the program computes the right answer and then aborts. milo-self does not treat passing a fixed array of non-Copy elements by value as a MOVE, so both frames believe they own it. The reference compiler is correct here (20/20 runs clean); this is milo-self only. **Nondeterministic from the outside, which is what makes it worth writing down:** the double free lands in the allocator, so the same binary aborts with SIGABRT after printing, or traps with SIGTRAP before its buffered output is flushed, depending on heap state -- it looked like a flaky fixture rather than a memory bug until ASan named it (`attempting double-free`, freed in `total`, freed again in `main`). That flakiness is also how it briefly got ratcheted INTO `tests/selfhost-manifest.txt` on a lucky pass and turned the CI sweep red; `--write` now confirms a newly-passing fixture several times before claiming it. Fixture: `tests/fixtures/arrayOfGenericElements.milo`, deliberately NOT in the manifest. **ROOT CAUSE, 2026-08-20:** not a missing wrapper. src-milo DELIBERATELY lowers a fixed array onto the Vec runtime (`astTypeStr`: `if ty.isArray { base = "Vec<" + base + ">" }`, so the {ptr,len,cap} header lets len/index/iterate/index-assign reuse the Vec paths), where the reference compiler emits a real `[2 x %Vec]`. So `total(a: [Vec<i64>; 2])` compiles to `@total(%Vec %a)` against the reference compiler's `@total([2 x %Vec] %a)`, and the bug is that array-as-Vec meets by-value ownership: the callee owns and drops the Vec, and the caller drops the array literal too. Fixing it means either making the by-value transfer a move under that representation, or giving src-milo real fixed arrays. Adding the array wrapper to `resolveMonoType`'s generic branch (which does drop it) was TRIED and is not the fix: it does not touch this path at all, since `total` is not generic, and it contradicts the Vec lowering everywhere else. | `src-milo/codegen/types.milo` (astTypeStr array lowering), `tests/selfhost-manifest.txt` |

## Tier 2 — strategic (high ROI, higher effort) — plan & invest

| # | Item | ROI | Effort | Why / unblocks | Ref |
|---|------|-----|--------|----------------|-----|
| 1b | **`pub` has one level, and packages need two — decide BEFORE packages ship** | H | M | Visibility is a single bit today: file-private, or `pub` and visible to everything. That was the right call for a file boundary and it is the wrong shape for a package boundary. A package whose helper is used by two of its own modules must mark it `pub`, which also hands it to every consumer — reintroducing exactly the *"every internal helper is somebody's dependency"* failure that `docs/plans/package-manager.md` opens by citing as the reason `pub` had to exist at all. Solved one level down, reopened one level up. **The deadline is what makes this Tier 2 rather than someday:** adding a second level after packages ship is a breaking change to every library that guessed wrong in the meantime, and the mangling pass (P0) is the thing that will have to carry the distinction, so it wants deciding before that lands rather than after. Neither the package-manager plan nor the `pub` worksheet mentions a package-scoped level — this is a genuine gap, not a deferred entry. Shape is the open question, not whether: `pub(pkg)` (Rust's `pub(crate)`, explicit and greppable, costs a parse form) vs. making `pub` mean package-visible and adding a louder marker for the true public API (fewer keywords, silently re-scopes every existing `pub` in std). | `docs/plans/package-manager.md` §"Private by default", `src/visibility.ts`, `src/mangle.ts` |
| 2 | **`std/smt` can't decide past ~2^62** | M | H | **No longer unsound** — `combineTerm` detects the i64 overflow and the verdict degrades to `unknown` instead of a false `proven`. What remains is capability: Fourier-Motzkin multiplies constants, so anything near 2^62 overflows and the answer is lost. That is why `verify.ts` omits i64/u64 param ranges — with them on, a genuinely broken call reports `unknown` instead of its counterexample. Needs wider arithmetic (i128/bignum) in the elimination. | `std/smt.milo combine`, `verify.ts INT_RANGES` |
| 3 | **Prover models ints as unbounded, params only** | M | H | `verify.ts` now asserts each int param's real range, which killed the `fpMul` false alarms — but **intermediate arithmetic carries no range**, so `error = setpoint - measured` (two i32s) can reach -2^32 in the model and refute a call that no real i32 could. That is the whole `pidStep` baseline entry. Needs range-carrying arithmetic or a bitvector model. **TRAP — do not "fix" this by asserting local ranges the way params get them.** Params are safe to bound because they are inputs. A DERIVED value is not: assert `(>= error -2147483648)` on `error = setpoint - measured` while the arithmetic is modelled as unbounded `Int`, and every overflowing (setpoint, measured) pair becomes UNSAT — silently excluding exactly the inputs that matter. The prover then reports an unconditional `proven` for a claim that only holds when nothing overflows. That is a FALSE PROOF, the same class as the `combine()` overflow that made std/smt report UNSAT on a wrapped row (see tests/prove/overflowNoFalseProof.milo). The two honest routes stand: range-carrying arithmetic (derive the bound, do not assume it) or a bitvector model (model the wrap). | `verify.ts intRangeAssumption`, `miloTypeToSmt` |
| 4 | **Extern fn parameters are unverified** | M | H | The audit now covers **return types** on both platforms (CI runs it on macOS + Linux), and std is clean — so `@cSig`'s remaining gap is the Milo-param ↔ C-param mapping, which `cSigGuard` documents as unchecked (its two asserts cover the stated-signature-vs-header claim and the return type only). Needs a C parser to introspect a function type's parameter list; `__builtin_types_compatible_p` on the whole signature is exact but only when the C signature is hand-written (`@cSig`). Nothing cheap left here. | `scripts/audit-extern-returns.ts`, `src/codegen.ts cSigGuard` |
| 5 | **Cross-compiling hosted targets needs a sysroot** | M | H | `--target` now reaches clang and a cross build fails loudly with a hint (was: silently emitted a host binary and reported success). Actually *doing* it needs a target linker + sysroot; the compiler has no `-I`/`-isysroot`/`--sysroot` notion at all. Until then, build on the target. Also what blocks cross-target `@cLayout`/`@cSig` verification, which warns and skips. | `src/main.ts clangTargetFlags`, `linkIR` |
| 6 | **Generalize the Vec combinators to slices + arrays** | M | M | **DONE 2026-08-17 — both halves.** A slice needed no emitter at all: `&[T]` already carries the `%Vec` layout, so it was a re-tag, gated behind a whitelist (`SLICE_COMBINATORS`, `src/types.ts`) because the same paths carry `push`/`pop`/`insert`/`sort` and a slice is a non-owning view (`tests/errors/slicePush.milo` locks that). A FIXED array could NOT be re-tagged — `[N x T]` is an inline layout — so both boundaries materialise a full-range slice, which is the `VecSlice` node `a[0..N]` already produced: lowering wraps the receiver for `a.sum()`, and the checker marks the argument for `total(a)` with lowering wrapping it in `lowerExpr` (one place, so every call shape is covered). `clone` is excluded from the array set on purpose — cloning a fixed array must yield an array, and the re-tag would silently change the result type. **The mistake worth keeping:** widening only the CHECKER for slices made the compiler accept `s.sum()` and then die in lowering with `unsupported method call: sum` — a crash, not a diagnostic. The checker and the lowerer now read the same two lists for exactly that reason. Fixtures: `sliceCombinators.milo`, `arrayCombinators.milo`. Lazy/fusing adapters remain declined (Graydon #2; measured numbers in Tier 2 #24). | `src/types.ts` (SLICE_/ARRAY_COMBINATORS), `checker.ts`, `lower.ts` |
| 7 | **Byte views for I/O interop** | H | M | Vec/string/array slicing all shipped (`arraySlice`/`vecSlice`/`stringSlice` fixtures). Remaining: generalized byte views for I/O/`Buffer`/`ArrayBuffer`. Unblocks the zero-copy form of #8 (JSON byte-feed). | roadmap: Borrowed slices / byte views |
| 8 | **JSON incremental byte-feed** | H | M | `jsonPull` shipped (string-backed). Remaining: incremental feed for unbounded input (socket/multi-GB) — a reader layer over the same tokenizer. Hands out copies until #7 (byte views) lands. | `std/json.milo jsonPull`, roadmap: Standard Library |
| 9 | **Dynamic `splitMut` (the N-way, runtime-bound split)** | M | M | **RE-VERIFIED 2026-08-16: accurate.** A `&mut [T]` parameter view mutates its caller's Vec in place (measured), and no `splitMut` exists anywhere in the API surface — so the shipped half is shipped and the dynamic N-way split is genuinely all that is left. **Stage (a) SHIPPED and most of (b) with it — re-verified 2026-07-31, this entry was badly stale.** `&mut [T]` param views work (`fn bump(s: &mut [i64])`, fed `v[0..2]` bare), and *disjoint mutable windows into one buffer already work today* for literal ranges: `two(v[0..2], v[2..4])` compiles and runs, while an **overlapping** pair is rejected at the call site (commit 49021315). So the parallel-over-one-array wall is down for the static case — image tiling and chunked checksum can be written now. **What is left is only the dynamic form:** `splitMut(&mut v, n)` for a runtime `n`, which second-class refs cannot express as a returned tuple of `&mut [T]` — it needs a callback shape, `splitMut(xs, mid, |a, b| …)`, plus prover-discharged disjointness when the bounds are not literals. Range disjointness (`hi_k <= lo_{k+1}`) is linear scalar arithmetic, exactly what `milo prove` handles; per-element disjointness stays out of reach (no `IndexAccess` reasoning, Tier 2 #3). Still deliberately **not** an ECS scheduler — read/write-set inference belongs in a library once the split is sound. | `checker.ts` (ref-param coercion arm, overlapping-slice-arg check), `verify.ts` |
| 10 | **Flow-sensitive invalidation tracking** | H | H | Compile-time catch of aliased mutation / use-after-invalidate — the aircraft-grade safety tier. | roadmap.md, safety-roadmap.md |
| 11 | **Compile-time reduction** | H | H | **PROFILED 2026-07-16 — no longer diffuse.** Self-host (20k LOC): frontend 0.38s, clang -O2 **7.3s (95% of build)** on 493k lines of IR from 905 fns. NOT generics — only 8 monomorphized instances exist, so de-monomorphizing buys nothing. Three concrete levers, in order: (a) `src-milo/codegen`'s string-compare dispatch chains — `genMethodCall` alone is 50k IR lines (10% of total), top-3 fns are 25%; interned method IDs / jump table is a *Milo-source* fix. (b) `String` by-value shreds structs: 90k insertvalue/extractvalue churn. (c) MIR to pre-shrink load/store before LLVM (Tier 3). **(d) parallel codegen units — SHIPPED 2026-08-04 (`src/cgu.ts`, `--cgus=<n>`, auto above 20k IR lines; 1.2–2.0x end-to-end, 5x on the clang step alone, 824/824 tests green with the splitter forced on every compile). Levers (a)-(c) remain open.** Original measurement: clang parallelises across processes almost linearly here — 4 × `-O2 -c` on the same 115k-line module is 3.89s serial vs **1.13s parallel (3.4x)** — and we hand it ONE module and ONE clang. Splitting the emitted IR into N CGUs, compiling concurrently and linking the objects attacks the 95% term directly, and unlike per-module incremental it needs no resolver rework: it is a *backend* split after monomorphization, when the full function list is in hand. Cost is linkage bookkeeping — of 921 `define`s in a real module 865 are `internal`, so anything referenced across a boundary must lose `internal` or be duplicated, plus partitioning 1,415 globals and 129 type decls; `src/mangle.ts` already gives stable cross-CGU names. This is also the single prerequisite that decides whether a Milo-hosted compiler is iterable at all — see [plans/milo-first-inner-loop.md](plans/milo-first-inner-loop.md). | memory: papercuts-from-hades, scratchpad profile 2026-07-16, [plans/milo-first-inner-loop.md](plans/milo-first-inner-loop.md) |
| 19 | **Generic inference stops at the top level of a param type** | M | M | **Mostly FIXED 2026-08-04** — `fn f<T>(v: &Vec<T>)` called with a `Vec<i32>` used to report `cannot infer type parameter 'T'`, so every generic-over-container API needed a turbofish at each call site; the argument loop now falls back to the same structural unifier `inferTypeParamsFromHint` already used for return hints and closure signatures (`checker.ts`, fixture `genericContainerInfer`). **RE-VERIFIED 2026-08-16 and the residue is NARROWER than it read.** Inference already covers the ordinary case: `let q = Pair { a: 9, b: "y" }` with no annotation infers both parameters from the field values, and `fn f<T>(v: &Vec<T>)` called with a `Vec<i64>` resolves. What does not exist is the SPELLING `Pair<i64, string> { … }`, in any position — and it never did: with one type argument it reported `undefined variable 'Wrap'`, with two it reported `unexpected token ','` at the type-argument comma, neither of which names the problem. **The diagnostic is fixed (2026-08-16)**: it now says the form is not a struct literal and gives both ways out (drop the arguments and let the fields infer them, or annotate the binding). Recording one thing that fix exposed — `tests/errors/recursiveGenericDepth.milo` was WRITTEN in this form and passed only because the checker recovered from `undefined variable` and reached the depth cap it was actually testing; a directed parse error stops earlier, so the fixture now uses the annotated spelling. **What is genuinely left** is whether the spelling should exist at all: `StructLit` carries no type arguments, so supporting it means the AST, the checker and lowering, and the only case inference cannot already handle is a parameter no field mentions (`struct Phantom<T> { n: i64 }`). Decide that before building it. Same family as T1 #5. | `src/checker.ts` (generic call inference), `src/parser.ts` (struct-literal turbofish) |
| 12 | **Quantifiers + a sequence theory** | M | H | The one STRUCTURAL gap left in the prover, and the reason binary search cannot be specified in Milo at all: `forall|i,j| v[i] <= v[j]` (sortedness) and `exists|i| k == v[i]` are not merely hard to prove, they are **unstateable** — there is no binder syntax and `IndexAccess` has no translation. Everything shipped in the 2026-07-24 contract pass (`old`, `decreases`, struct + for-in invariants) was the local, syntactic half; this is the half that needs new theory. **Cost is trigger selection**, which is where Dafny/Verus users actually burn their days and which contradicts the "no annotation burden" axiom. Milo-specific multiplier: `std/smt` is Fourier-Motzkin over linear integers and **cannot represent quantifiers at all**, so any sequence theory is z3-only and makes the in-box solver permanently second-class rather than merely weaker. **If it is done, do the bounded form first** — `forall i in a..b { P(i) }` — because the range supplies the instantiation set and sidesteps trigger selection entirely; that covers array-property specs (sorted, all-positive, permutation-free) which is most of the real demand. Unbounded `forall|x|` is a separate, much larger decision. Prerequisite either way: an `IndexAccess`/select-store model, which is worth having on its own. | `src/verify.ts exprToSmt` (IndexAccess), `std/smt.milo`, `docs/verification-roadmap.md` |
| 13 | **`@reads` / `@writes` (SPARK's `Global`)** | M | M | Ownership + second-class refs already give Milo most of what SPARK's `Depends` buys — a `&mut` parameter is the only way to write through an argument, which is exactly why havoc-on-`&mut` is both sound and precise. The residue is **module-level `var`**: a call to any function that might touch a global forces the prover to assume the worst. An explicit reads/writes set turns that into a frame condition. Lower priority than `@pure` (Tier 1 #3), which is the degenerate case of the same idea. | `src/verify.ts collectMutations` |
| 14 | **`old` at loop entry (SPARK's `'Loop_Entry`)** | L | M | An invariant that names the value a variable held BEFORE the loop, not before the current iteration — e.g. `invariant total >= old_loop(total)`. `old()` on functions shipped 2026-07-24 and the machinery generalizes (a second pre-state env threaded through the same lowering), but the demand is narrower: most useful invariants are about the accumulator's absolute value, not its relation to the entry state. | `src/verify.ts OLD_ENV`, `LoopObligation.entryEnv` |
| 15 | **Niche optimization for enum layout** | M | M | **MEASURED 2026-07-24, cross-language benchmark run.** `enum Tree { Leaf, Node(Heap<Tree>, Heap<Tree>) }` lays out as 24 bytes (i32 tag + 4 pad + two ptrs); Rust's identical type is **16** (`size_of` verified) because it encodes `Leaf` in the null-pointer niche of a `Box` field. That extra 8 bytes per node is the *entire* Milo-vs-C gap on `benchmarks/binarytrees`, isolated by benchmarking C variants that change one factor at a time: zig 16B+`smp_allocator` 1.9ms, C 16B+malloc 2.2ms (+15% = allocator), C **24B**+malloc 2.6ms (+18% = node size), Milo 24B+malloc 2.7ms (+4% = `Heap<T>` codegen). So `Heap<T>` itself is fine and **this is not an allocator problem** — do not chase a custom allocator, the residual 15% is libc malloc and is a link-time mimalloc/snmalloc swap if it ever matters. Payoff is broad, not benchmark-local: every `Option<Heap<T>>`/`Option<&T>`/pointer-payload enum in std and the self-hosted compiler shrinks, and HIR/AST sum types are the self-host's dominant allocation. Needs a niche-tracking notion in `TypeKind` layout (which payload fields have forbidden bit patterns), tag-read/tag-write lowering that reads the niche instead of a tag word, and drop glue + `match` to agree on the encoding. Non-null `Heap<T>` is the only niche worth doing first; bool/enum-range niches are a later generalization. | `src/types.ts` layout, `src/codegen.ts` (enum tag emission, `milo.drop.*`), `benchmarks/README.md` |

| 16 | **Bounds-check elision via proof** | H | H | **MEASURED 2026-07-31 on examples/games/flight — the number to beat is 17–22%.** **Benchmark moved 2026-08-02:** flyby's renderer is on the GPU now, so these loops no longer run in the game. They live in `benchmarks/softraster/` (`milo run benchmarks/softraster/main.milo --release`), which exercises the same `bandRaster`, `spot`, `blurPass` and composite against synthetic geometry — 4.7 ms raster + 6.0 ms post at 1280x720. Re-baseline against that, not against the numbers quoted here. Stripping all 362 bounds checks out of the emitted IR by hand takes the flyby frame from 23ms to 18–19ms, output byte-identical, five runs each. Stripping the 498 overflow checks on top buys **nothing** (still 18–19ms), so overflow checking is not the cost — subscripts are. Three cheap pieces landed already and are NOT the answer: the failure paths are now one cold `noreturn` call instead of inline printf+fflush+abort at every site (rasterCamTex carried 24 of each); `for i in 0..v.len` with an unresizable body elides its check outright (`pushProvenInRange`); and loop-invariant lengths are loaded once in the preheader instead of per access (`pushHoistedLens`, present() 26 len-loads -> 14, spot() 6 -> 2). **All three measured zero.** LLVM already eliminates the simple `for i in 0..v.len` shape by itself, and the length loads were L1-warm. What is left is the affine case and only the affine case: `c.hdr[o]`, `c.hdr[o+1]`, `c.hdr[o+2]` where `o` is a loop-carried accumulator and the length lives behind a `&mut Canvas`. That needs a real range lattice over derived values (i = A*x + B, A/B loop-invariant, then one guard on the max index in the preheader), which is this entry. **Semantics trap:** hoisting a check to the preheader aborts BEFORE side effects the checked iterations would have produced. Either accept that for a program that is already going to die, or prove-and-delete rather than hoist-and-check — the latter is what the landed elision does. **Also the prerequisite for #17**: a 4-lane load wants one check per vector, not four, and stripping checks is what lets LLVM vectorise the loops at all (verified: with checks removed the blur loop comes back as `<4 x float>`; with them in, it does not). | [docs/plans/bounds-check-elision.md](plans/bounds-check-elision.md), `codegen.ts emitBoundsCheck`, `verify.ts` |
| 17 | **SIMD vector types** | H | H | **RE-SCOPED 2026-07-31 with a profile — the rasteriser is NOT where the time goes.** **Benchmark moved 2026-08-02:** flyby's renderer is on the GPU now, so these loops no longer run in the game. They live in `benchmarks/softraster/` (`milo run benchmarks/softraster/main.milo --release`), which exercises the same `bandRaster`, `spot`, `blurPass` and composite against synthetic geometry — 4.7 ms raster + 6.0 ms post at 1280x720. Re-baseline against that, not against the numbers quoted here. `sample` over 300 rendered flyby frames: `spot` 24.6%, `blurPass` 23.6%, `rasterCamTex` 20.7%, `fillRect` 8.5%, `rasterCamG` 8.1%, `rasterCam` 2.9%, plus ~26% in present()'s composite inlined into main. Post-process is ~60% of the frame and the triangle rasterisers are ~32%. The post-process loops are dense, uniform-stride float loops with no masks and no gathers — LLVM's existing auto-vectoriser handles that shape unassisted **once the bounds checks are gone** (verified on the emitted IR: strip the checks and the blur loop vectorises to `<4 x float>` with strided loads; leave them in and it refuses with 'Cannot vectorize early exit loop with more than one early exit'). So **#16 lands first and may collect most of this entry's win with no new language surface at all.** What genuinely needs explicit vectors is the rasteriser inner loop: a data-dependent depth test and a texel fetch, which is a masks-and-`select` problem. Correction to the original entry: const generics are NOT a gate — `Simd<T,N>` generic over lane count is a library nicety; concrete `f32x4`/`i32x4` builtins deliver the rasteriser case with zero new generic machinery, and Milo has no const generics anywhere today. The 3–5× claim remains unmeasured and should be re-derived against the profile above (32% of frame at best 4x = ~24% total), not assumed. Original design notes below still stand. `grep -n 'SIMD\|<4 x ' src/` returns nothing — there is no way to express data parallelism at all, so a rasteriser inner loop shades one pixel at a time and concedes a real **3–5×** to a C++/Rust renderer using NEON. Named as the one place another language is currently the right answer for a hot loop. **It is an intrinsics gap, not a design one**: a `Simd<T, N>` is a Copy value type with no pointers and no aliasing, so it needs zero new checker rules and does not touch ownership, second-class refs, or the proof story. Take the portable model (Zig `@Vector` / Rust `std::simd`) lowering to LLVM's native `<N x T>`, **not** a `vld1q_f32` NEON surface — target-specific intrinsics would multiply the platform-suffix problem across hundreds of names, and every arm would have to export the same surface anyway, which is what a portable vector type already is. LLVM does instruction selection, so the work is type plumbing (`TypeKind` gains `simd`, `llvmType` maps it) plus alignment discipline. Masks + `select` are **not optional polish** — without them a z-buffer test cannot be expressed and the motivating case does not work. Out of scope: auto-vectorisation (LLVM's already runs at -O2), runtime feature detection, scalable vectors, prover support. Risks: alignment (a real crash, not just slow), and the `extern` ABI — reject `Simd` in extern signatures first rather than emitting a wrong-ABI call that happens to work on one platform. | [docs/plans/simd.md](plans/simd.md), `src/types.ts`, `src/codegen.ts`, `src/abi.ts` |

| 18 | **Per-module namespaces (kill the flat merge)** | H | M | **STAGE 1 SHIPPED 2026-08-15 — the collision error class is gone for user modules.** Two modules may each define a private `fn tone`, `struct Node`, global, trait or type alias now, and each call site reaches its own module's body (verified: 11 and 1000 from `+1` and `*100` bodies of the same name). **Narrower than this entry proposed, and the reason is worth keeping**: mangling every private name works but a mangled name is not only a symbol — `print` of a struct emitted `printContainers$User` and 14 error fixtures stopped matching their `@error:` text, 24 failures in total. So the shipped pass renames only names that ACTUALLY collide; a program with no collision is byte-for-byte what it was (866/866, output unchanged). `pub` names are untouched, so no import binding had to be rewritten and two modules exporting the same name with different bodies is still an error — a genuine ambiguity for anyone importing both. Carve-outs: `extern`, `@externalLinkage`, `main`, `@cName`, `@cLayout`; packages and `std/` skipped. Zero regressions across fixtures, examples, packages, LSP, src-milo, and 87 entry programs in 12 sibling repos (9 of those fail identically on the baseline — pre-existing API drift). **LEFT**: a collided private name still reaches diagnostics, `print` output and DWARF mangled — fixing that needs a display name beside the symbol, and it gates ever widening the pass beyond collisions. LSP go-to-definition on a collided name is untested. std stays flat (stage 4). | [docs/plans/module-namespaces.md](plans/module-namespaces.md), `src/mangle.ts`, `src/resolver.ts`, `tests/modules.test.ts` |

| 20 | **No closed-world dead-`pub` analysis over `std/` and `src-milo/`** | H | M | **FILED 2026-08-15 from [astral-sh/hawk](https://github.com/astral-sh/hawk); its `docs/architecture.md` is the design writeup worth reading before starting.** Hawk exists for Ruff and uv, which have Milo's exact shape: a large internal surface that is `pub` only because some boundary demanded it, with the project itself as the only client. Milo's version is *easier*, because most of hawk's complexity is plumbing Milo does not need (`rustc_private` toolchain pinning, one crate per compilation, JSON fragments merged across many `cargo check` invocations, path-spelling normalization so one file compiled under two working directories still merges). Milo compiles whole-program in one process against a flat namespace, so the reachability closure is a graph walk over HIR that already exists. Four semantics do carry over, and all four are needed for the result to be a triage list rather than noise: **(a) liveness and visibility-requirement are separate closures** (hawk's table: not live anywhere *and* not required-public = dead surface; live but not required-public = over-broad visibility. Conflating them reports items that must stay `pub` for a signature or re-export even though nothing calls them). **(b) production and non-production get separate closures** (a std fn only `examples/`, `tests/`, or `benchmarks/` reaches is dead for shipped code but must keep `pub`). **(c) conservative roots establish liveness without requiring visibility**: `@externalLinkage`, `main`, trait/interface impl members, `@embedFile` targets. **(d) dead findings stay report-only; only visibility *reductions* auto-fix** (hawk's reasoning: narrowing dead surface instead of deleting it just relocates the problem into the ordinary dead-code lint). **The Milo-specific blocker, which must be solved before this can run in CI:** the per-OS std files (`crypto.{darwin,linux,windows}.milo`, `environ.*`, `event.*`, `platform.*`) are Milo's `#[cfg]`. A single-target run reports every Windows-only `pub fn` as dead when run on macOS. Either union liveness across all supported targets before reporting, or preserve visibility for any decl absent from the analyzed target (hawk does the latter for cfg-disabled struct fields). `src/lower.ts` already carries the dead-platform-branch concept for codegen. **Land it as a `scripts/` audit over `std/` first, not a user-facing warning** — the first run's output is a triage list, not a diff. **Deliberately not copied from hawk:** the `pub(crate)`/`pub(super)` gradations, uniform-field-visibility groups, and the fix-replan-recompile loop. Those exist because Rust has a visibility lattice and separate crate compilations; Milo's visibility is one bit and one shot, so a fix is a token deletion with nothing to re-plan. Revisit only if Tier 2 #1b adds a second level. | `src/visibility.ts`, `src/hir.ts`, new `scripts/` audit, [breaking-changes.md](breaking-changes.md), Tier 1 #24, Tier 2 #1b |

## Tier 1 — landed 2026-08-03, residue only

- **The ownership fuzzer's remaining blind spots.** Updated 2026-08-14. The
  generator now reaches **27 of the 39** expression/statement kinds in `src/ast.ts`
  and nests them to depth 5 (`bun scripts/fuzz-coverage.ts` measures both axes;
  they move independently — a new shape moves the first number, nesting moves the
  second). It was 17/39 and depth 2 on 2026-08-13. What is left, and it is now a
  short list rather than a vague one: the eight owning kinds the census names
  (`ArrayLit`, `ArrayRepeat`, `CastExpr`, `IsExpr`, `Propagate`, `RangeExpr`,
  `UnaryOp`, `UnsafeBlock`), enum payloads beyond `Option`, and Vec-of-struct.
  A move-capturing closure that ESCAPES was excluded here while it was a known-open
  hole; it was closed 2026-08-16 (roadmap, Safety Hardening: `move` on an outer
  closure copies an inner borrowing closure's env pointer without owning it, and
  `retainsParam` could not see a capture at all), so this shape is now a candidate
  for the harness rather than a permanent red light.
  The pattern to expect is the one the `Drop` shape set: a missing shape is a
  missing bug class. The counter-evidence so far is that the ten shapes and the
  nesting added on 2026-08-14 found nothing in the compiler — one model bug in the
  harness, and that is all (`scripts/fuzz-ownership.ts`, `scripts/fuzz-coverage.ts`)
| 21 | **Move `@derive(Json)` out of the compiler and into `std/`** | M | M | **UNBLOCKED 2026-08-17 — the blocker (blanket impls) shipped, see #22.** A complete recursive JSON serializer with CONTAINER fields is now ~30 lines of Milo and needs no compiler change: `tests/fixtures/deriveTemplateJson.milo` covers `i64`/`bool`/`string`/nested struct/`Vec<string>`/`Vec<i64>`. Dispatch does the type-directed work a proc macro would otherwise do, which is why the template language still needs no conditionals. **What is still NOT ported, and why this entry stays open:** the built-in derive also carries the whole `fromJson` DECODE direction, narrow-int range checks on decode (a `u8` field must reject 300 from the wire, not store 44), fixed arrays, HashMap key ordering, and `@json("…")` wire renames. Encode is the easy half. Do not ship a subset `Json` into `std/` — a second, weaker codec under almost the same name is worse than the special case it replaces. | `src/derive-json.ts`, `tests/fixtures/deriveTemplateJson.milo`, `tests/fixtures/blanketImpl.milo` |
| 22 | **Generic dispatch: a method's own type parameter, and blanket impls on builtin containers** | H | M-H | **BOTH SHIPPED 2026-08-17.** (a) **A method carrying its own type parameter** (`fn map<R>(self, f: (&T) => R): R`) was never inferable — impl methods are monomorphized EAGERLY in `monomorphizeStruct` on the STRUCT's parameters only, so a method-level `R` survived as a literal type name and the call reported *"expected (&i64) => R, got (&i64) => i64"*, naming a type nobody declared. Methods with their own parameters are now kept as templates (`genericMethods`) and instantiated per call, the lifecycle `monomorphizeFn` already gave a generic free function; the two levels compose, so a method on `Arena<T>` arrives concrete in `T` and generic only in `R`. It also needed the structural unifier to descend into FUNCTION types, which it never did — that arm is what lets a callback carry a type parameter at all, and it helps generic free fns taking callbacks too. (b) **`impl Trait for Vec<T>`** parsed, landed in `genericImpls` under the bare name "Vec", and was never instantiated: `monomorphizeStruct` does that job and a builtin container is not a struct. Now instantiated per ELEMENT type under a mangled name (`Vec_i64`) — the mangling is what keeps two of them apart, since `resolveMethod` is keyed by a bare type name. Nested `Vec<Vec<i64>>` resolves by instantiating the same template twice. (c) **`HashMap<K, V>`** had the identical hole and is closed the same way, instantiated per key/value pair; an impl whose parameter count does not match the container's is skipped rather than bound to the wrong positions. **LEFT:** `std/` cannot USE any of this while it must stay compilable by the frozen `milo-self` — which is what still blocks giving `arenaWith` a method form (see #22's original motivation). | `src/checker.ts` (`genericMethods`, `monomorphizeMethod`, `instantiateContainerImpl`, `inferTypeParamsFromHint`), `tests/fixtures/genericMethod.milo`, `tests/fixtures/blanketImpl.milo` |
| 23 | **Type-enforced GC safepoints — the substrate a collector written in Milo needs, and the one Rust punts to `unsafe`** | H | H | **FILED 2026-08-17 from the milojs exchange, and it is the question that survived it.** The Handle-vs-index argument dissolved (see #22 — milojs is mid-migration and its append-only AST correctly uses newtypes, not handles). What did not dissolve: the collector bug that week was a MISSING ROOT, not a stale reference — `await Promise.reject(new Error("bad"))` awaits a temporary reachable only from an evaluator local, and draining microtasks collected it, so the catch block got an `Error` with no message. No handle scheme catches that. The rule that would is *an unrooted heap value may not be live across any call that can collect*, i.e. `pushTemp` becomes an API rule rather than a convention. **Rust punts this to `unsafe` in every GC crate that has tried, and Milo starts from a better position — worth checking before assuming it inherits the difficulty.** Three pieces already exist and none of them is a new type system: **(a) second-class refs** mean a raw `&T` into the heap cannot be stored or returned at all, so the only values that can be live across a call are owned ones and indices — a much smaller surface than Rust's, where any `&'a T` can be threaded anywhere a lifetime permits. **(b) The poison mechanism is built.** `freezeViewSource` marks a binding borrowed with PATH granularity and `errorIfFrozen` rejects conflicting uses while it is live; a view of `r.items()` blocks writes to `r.data` and nothing else. "Invalid after a safepoint" is that same per-binding flag with a different trigger. **(c) The trigger is one bit, not an effect system.** Effect rows and capabilities were rejected for Milo (`docs/effects-and-capabilities.md`) and this does not need them: "can this callee collect?" is a single boolean computed by transitive reachability from whatever calls `collect()`, which is exactly the shape `retainsParam` already computes by walking callee bodies, and Milo compiles whole-program in one process against a flat namespace so the closure is a graph walk over HIR that exists (the same reason Tier 2 #20 is easier here than hawk was for Rust). **What is genuinely unresolved and must be decided first:** how the rule is stated at a boundary. `retainsParam` gets to fail closed because over-rejecting a closure is an inconvenience; over-rejecting "may collect" would poison every value across every call in an interpreter, which is unusable. So this needs the opposite default — an explicit marker on the collecting functions, propagated — and the cost of getting that wrong is a false proof of rootedness, not a false alarm. Do not start it as an inference pass. | `src/checker.ts` (`freezeViewSource`/`errorIfFrozen`, `retainsParam`), `docs/effects-and-capabilities.md`, milojs `docs/milojs-arena-safety.md` step 5 |
| 24 | **Combinator fusion — worth ~1.3x on a chain, and the premise usually given for it is FALSE** | L-M | M | **MEASURED 2026-08-17, prompted by an outside review claiming "eager map/filter means idiomatic code allocates per stage, so hot paths regress to while-loops".** That claim does not hold here, and the opposite does. On 1M i64, four consecutive runs of one program (cross-program numbers are useless — the same `map` measured 4270us in one binary and 1300us in another, so only variants timed together in a single run are comparable): **`v.map(f)` 1180-1333us, a hand-written `for-in` + `reserve` with the arithmetic INLINE 1729-1925us, the same loop calling a closure 2011-2230us.** The builtin beats hand-rolled code including the version with no call at all, so "the pretty version regresses to a while-loop" is backwards — the while loop is the slow one. Two reasons: `genVecMap` already emits ONE exact-size `malloc(len * elemSize)` (there is no per-stage growth realloc to eliminate for a single stage), and an indexed `v[i]` loop pays a bounds check the builtin's pointer walk does not. **What IS real is the CHAIN**: `filter().map()` builds and frees an intermediate buffer and walks the data twice. Against a hand-fused single-pass loop over 2M: 15335/7586/6394us chained vs 7911/6173/4664us fused (the first run of each is cold; warm ratio is **~1.3-1.4x**). So a pass that recognises adjacent `VecFilter`/`VecMap` in HIR and emits one loop is worth about a third on multi-stage chains and NOTHING on single stages. Decide with that number, not the premise: it is a real but modest win, on a shape (2+ stage chains on large vectors) that has to be shown to be hot before it earns a compiler pass. Related and NOT the same thing: lazy/fusing ADAPTERS are declined outright (Tier 2 #6, Graydon review decision #2) because laziness pays only through aggressive inlining and drags associated types into the trait system; this entry is the compiler-pass alternative, which needs neither. | `src/codegen.ts genVecMap`/`genVecFilter`, `src/lower.ts`, Tier 2 #6, [graydon-review.md](graydon-review.md) |

## Tier 3 — backlog (niche, deferred, or lower ROI)

Track in [roadmap.md](roadmap.md); pull up when a concrete need appears.

- **`extern struct` is misnamed — it means `repr(C)`, not "defined elsewhere"** — `extern fn` and `extern type` both declare something that lives on the C side; `extern struct SockAddrIn { … }` declares the layout right here, so `extern` carries a third, unrelated meaning in the same family. Rust splits these deliberately (`extern "C" fn` vs `#[repr(C)]`). An attribute would say it honestly, and `@cLayout` already sits on these declarations doing the C-side verification. Purely a naming/teaching fix — no semantics change, and nobody trips on it in practice, so it waits for a moment when the FFI surface is being touched anyway (`parser.ts`, `docs/language-reference.md` §C FFI)
- **`JsonObj.build()` returns `string`, not `Json`** — read/write paths disjoint; bridge later (`std/json.milo`)
- **Error boxing** — the `?` half of "error conversion" shipped: wrapping-variant auto-From converts the error type in `?` (`tests/fixtures/resultFromConversion.milo`, alias form too). What's left is `anyhow`-style boxing, which wants Heap\<Interface\> (roadmap.md)
- **Ranged integers L3** — branch narrowing (roadmap.md)
- **Heap\<Interface\>** — heterogeneous collections (roadmap.md)
- **C ABI layout control** — packed structs, alignment (roadmap.md)
- **Structured OS/syscall errors** — `errno` + context (roadmap.md)
- **Struct-by-value FFI stage 6** — define-side exported struct-by-value fns (memory: struct-by-value-ffi)
- **`-O2` codegen blowup + int-widening** — deferred from emulator work (memory: emulator-feedback-fixes)
- **Missing bindings** — `alarm`/`setitimer`, `setpgid`/`killpg`. (`execvp` shipped: declared in `std/os.milo`, used by `std/process` and `std/pty`.) (roadmap.md)
- **MIR** — optimization IR, post self-hosting (roadmap.md)
- **"The book"** — documentation/tutorials (roadmap.md). Cross-compilation is Tier 2 #5, not a separate item; the benchmarking harness shipped (`benchmarks/run.sh` + per-bench `results-*.md`).
- **Flaky `fmtCorpus` "sources are formatted"** — seen once (2026-07-24, e84f0732): Release's macOS `bun test` reported `tests/fixtures/softKeywordFromIn.milo` unformatted while the CI workflow's *identical* macOS job on the same commit passed, and a re-run of the failed job went green. Not reproducible locally — the file round-trips clean through a freshly built `fmt`, 3 full runs of `tests/fmtCorpus.test.ts` pass, and `emit-ir` on `fmt.milo` is byte-identical across runs, so it is not frontend nondeterminism. Next occurrence: capture the actual formatter output (the test only records the path), and suspect the `execFileSync` stdout capture under load before suspecting the formatter. (`tests/fmtCorpus.test.ts`)
- **~~Re-enable `noUncheckedIndexedAccess`~~ — DROPPED 2026-08-06 with the src/ freeze.** The
ratchet (`tests/uncheckedIndexRatchet.test.ts`, `tsconfig.indexed.json`) is deleted. It
existed to force ~900 unchecked `arr[i]` sites in the TS compiler down to zero, on the
argument that this was the one place safe Milo would be genuinely safer than the TS host.
That argument still holds — it just no longer points at `src/`. src/ is frozen bootstrap,
so paying the debt there buys nothing at runtime, and the guarantee it approximated is
enforced for real in src-milo, where an out-of-range index traps at the index site instead
of yielding `undefined` that corrupts a state frame three calls later. The one part worth
keeping was harvested: the 18 deferred undefined-handling decisions in `src/cgu.ts` are
now a port checklist in [plans/milo-first-inner-loop.md](plans/milo-first-inner-loop.md).
- **Const/value generic params** — Milo generics are type-only; bun-rs audit (2026-07-17) found 1,120 `const B: bool`-style sites (js_parser lexer monomorphizes over 9 const bools for perf). Workaround: runtime bools (perf loss) or hand-duplication. Only surfaced gap that's a real language feature; weigh against Ethos #3 when a concrete Milo need appears.
- **`std/decimal`** — scaled-i128 decimal for financial math; stdlib only, no compiler change ("every language discovers the long way that financial math is special" — graydon-review.md #4)
- **Explicit tail calls (`become`)** — never implicit TCO; LLVM `musttail` now works and Milo has no ABI-stability constraint; primitive for composable state machines (graydon-review.md #4)
- **`@embedDir(path)`** — embed a whole directory, then look files up at runtime: `let dir = @embedDir("assets"); let f = dir.get("index.html")`. Today it's one `@embedFile` call per file plus a hand-built `HashMap`, which every asset-serving program repeats (`examples/net/weather/app.milo` has ~8 of them, `termpair/server.milo` 5). Returns something map-shaped so lookup is a runtime string key; the directory walk happens at compile time. Passing a directory to `@embedFile` is currently a compile error pointing at the one-call-per-file workaround (`src/lower.ts`).
- **`@derive` on enums** — enums parse attributes but nothing consumes them; now a hard error rather than a silent no-op (`checker.ts validateAttributes`). Implement if wanted (`checker.ts processDerives` walks only structs, and skips generic ones).
- **`build-lib`/`emit-obj` give non-`pub` fns external linkage** — **header half FIXED** (`headergen.ts` now skips `!fn.isPub`, so the published C API is `pub`-only; `pub` is the decided boundary). What remains is the symbols: every entry-file fn still gets external linkage, so a caller can hand-declare and link a private helper. Cause is `userFnNames = new Set(program.functions.map(f => f.name))` (`src/resolver.ts:413`), no visibility filter, consumed at `codegen.ts:1397` for `internal`-vs-external. **Do NOT narrow that set** — it has two other jobs: `codegen.ts:1112` (fns outside it get sret-lowered, an ABI change) and `checker.ts:369` (`fnIsUserCode`, which attributes `unused-unsafe`-class warnings; narrowing would silently stop warning about non-`pub` user code). Add a separate `exportedFnNames` instead. Safe to do: `pub` is already enforced on imports (`'x' is private to y.milo`), so nothing cross-file can legitimately reference a non-`pub` name. (`src/resolver.ts`, `src/codegen.ts`)

## Concurrency & TUI findings (2026-07-15, tmuxClone/splitPty session)

Surfaced building a terminal multiplexer (`examples/terminal/tmuxClone.milo`) — the
primitives carried it, but these gaps are where the friction was. Ranked.

| # | Item | ROI | Effort | Detail | Ref |
|---|------|-----|--------|--------|-----|
| C4 | **`Process.wait()` blocks the whole scheduler thread** | M | M | `Process.wait` is `waitpid(pid, buf, 0)` — from a green task that wedges every other task on the thread, not just the caller. `Pty` already works around the sharp case (kill -> close master to unwedge -> reap). **The prerequisite is now done**: self-pipes are per-signal (they were one global, which cross-wired SIGWINCH onto SIGCHLD's fd), and the event-driven child wait is proven end to end (`selectChildExit.milo`; `examples/cli-tools/timeout.milo` for a real use). **The open design question, not a coding gap:** the green path needs a SIGCHLD self-pipe, and `installSignalPipe` sets a PROCESS-WIDE disposition — `Process.wait()` cannot quietly install one behind a caller who has their own SIGCHLD handler, and re-installing replaces their pipe. Needs a decision first (an opt-in `waitAsync`, a runtime-owned SIGCHLD reaper that fans out, or documenting "install it yourself"), then the conversion is mechanical. **DECIDED 2026-07-16:** the runtime owns SIGCHLD, installed at `schedulerEnsureInit`. **It must NEVER `waitpid(-1)`** — that would steal exit statuses from other reapers; the real ones in-tree are `std/pty.{darwin,linux}` (`waitpid(childPid, …, 0)`), `std/process` (`system()` and `Process.wait`), and **`examples/cli-tools/parallel.milo:99`, which itself blind-reaps with `waitpid(-1, status, 0)`**. (A previous revision of this note claimed parallel.milo didn't exist — wrong: it is under examples/, not std/.) `system()` blocks SIGCHLD and waits its own pid, so a per-pid WNOHANG reaper never races it; that is precisely why 'never -1' is the rule that matters. The reaper only wakes registered waiters, each doing `waitpid(itsOwnPid, WNOHANG)`. A caller installing its own SIGCHLD handler must be a HARD ERROR, not a silent replacement that hangs every green wait. **DECIDED 2026-07-17 — no separate `waitAsync`: the green path goes straight into `Process.wait()`, gated on `schedulerCurrent() != 0` (main-context wait stays the identical blocking `waitpid`).** This preserves `wait()`'s observable contract (block until exit, return status) and only changes the parking mechanism in green context — same shape as `readFd` (`std/os.milo:306`); it's a wedge fix, not a semantics change, so the incremental-API rule doesn't require a parallel API. `timeout.milo` migrates from its own SIGCHLD self-pipe (95160f8) to the runtime-owned path as part of this — mechanical debt, not a design question. | `std/process.milo Process.wait`, `std/runtime.milo schedulerEnsureInit` |

**Known limitation (not a bug):** on split/resize the daemon rebuilds every pane grid at the new width (shells repaint via SIGWINCH), so on-screen content of *other* panes is cleared at that moment (still live in each shell's own history). True content reflow across a resize is a later polish.

## Ergonomics findings (2026-07-16, ugly-code audit)

| # | Item | ROI | Effort | Detail | Ref |
|---|------|-----|--------|--------|-----|
| E1 | **Named enum-variant fields** | H | M | `ForEach(string, Option<string>, TypeKind, Option<TypeKind>, Heap<HIRExpr>, string, Vec<Heap<HIRStmt>>, Option<Span>)` needs a trailing comment decoding the slots. Hits the self-hosted compiler hardest (HIR/AST are all sum types). Rust-style `ForEach { varName: string, ... }`. **GREENLIT 2026-07-17 as a language feature (Ethos #2, readable sum types) — but it is a real addition, not a papercut: parser + checker + formatter + LSP (definition-of-done), and it queues behind the drop-glue slice-2 leak fix.** | `src/hir.ts HIRStmt.ForEach` |
| E2 | **Raw-pointer sugar: `ptr == null` + `ptr.offset(n)`** | M | L | Null check today is `dir as i64 == 0 as i64` (both sides cast); pointer arithmetic is `(ent as i64 + nameOff) as *u8` (three casts). Recurs across `fs`/`cstr`/`env`/`runtime`/`sync`/`crypto`. Confined to the FFI seam, but the ugliness is accidental, not deliberate friction. | `std/fs.milo readDir` |
| E3 | **Type-alias hygiene sweep** | M | L | Language has `type X = ...` (`TypeAlias` in `src/ast.ts`); `std/` + `src-milo/` use it zero times. `Vec<Heap<Stmt>>` is spelled verbatim ~37× (13 of those inside `Option<...>`) — one `type Block = Vec<Heap<Stmt>>` kills it. Pure code debt. | `src-milo/checker/stmt.milo checkReturn` |
| E4 | **Codegen context struct** | M | M | `locs/sigs/retTy` + 4 label params copy-pasted through every `gen*` fn (`genForInHashMap` takes 13 params). Fold into a `GenCtx`. | `src-milo/codegen/stmt.milo genForInHashMap` |
| E5 | **`jsonParseValue` err flag → `Result`** | L | L | Parser threads `err: &mut bool` through its recursive signatures instead of `Result<i64, E>` — predates typed-errors migration. Scratch-vec design itself is deliberate zero-alloc, keep it. | `std/json.milo jsonParseValue` |
| E6 | **Opt-in typed index: a newtype handle indexes its pool without `.idx`** | M | L-M | **Motivated by real shipped code — the `milo-language/yaml` 0.2.0 newtype migration.** Newtyping pool handles (`struct NodeId { idx: i64 }`) made the public API pool-safe (`strs[nodeHandle]` is a compile error) but left **55 `.idx` reads** at internal indexing sites (`nodes[h.idx]`). The ceremony is erasable: mechanics are ~12 lines — accept a single-i64-field struct at the index gate (`checker.ts:~4605`), and desugar `vec[h]` → `vec[h.idx]` in lowering (`lower.ts:~733`, existing FieldAccess path); codegen untouched. **The hard part is a language-design decision (yours), and it must tie a KEY to a COLLECTION, not just mark a key.** A blanket "any single-i64-field struct indexes any Vec" is UNSOUND (a `Meters{v:i64}` would index the node pool). But a marker on the KEY alone (`impl PoolIndex for NodeId {}`) is ALSO unsound — it would let `NodeId` index `strs` too. Soundness needs the COLLECTION to declare its key type: Rust's per-collection `Index<K>` — `nodes` accepts only `NodeId`, `strs` only `StrId`. Options: (a) an `Index<K>`/`IndexMut<K>` trait implemented on a thin pool-wrapper type (`impl Index<NodeId> for NodePool { … }`) — sound, but that wrapper is basically SlotMap/arena minus generations; (b) a typed-key collection in std (`TypedVec<K, V>`) that only `K` indexes — same idea, library not language. Either erases the `.idx` while keeping `strs[nodeId]` a compile error. Do NOT ship a key-only marker or a blanket auto-coercion — both re-open the footgun. Evidence file: `yaml lib.milo` (the canonical pool-of-indices use case is where the noise concentrates). | `checker.ts` array-index gate, `lower.ts` IndexAccess, `yaml/lib.milo` |

## std/os layering (found 2026-08-23, predicted failure observed 2026-08-24)

| ID | Item | ROI | Effort | Notes | Where |
|----|------|-----|--------|-------|-------|
| L1 | **`std/os` mixes libc with OpenSSL, poisoning `-O0` links** | H | M | `std/os` declares the libc memory primitives (`malloc`, `free`, `memcpy`, `memset`) AND 22 OpenSSL externs AND TLS helper code. Any module importing it for malloc drags the SSL symbols in. At `-O2` they are dead-stripped; at `-O0` they are not, so on a platform with no OpenSSL in the sysroot the link fails naming symbols the program never mentions. **Reproduce:** `MILO_WINDOWS_SDK=~/.xwin PATH=/opt/homebrew/opt/llvm/bin:$PATH ./milo build <file importing std/shard> --target=windows-x64 --debug` gives 5 undefined `SSL_*`. **This was predicted and then happened:** the entry originally noted that `std/shard` carried the bug but escaped CI because every shard fixture is `-O2`, and that the class would stay invisible until an `-O0` fixture reached it. On 2026-08-24 `tests/runtime-errors/shardWindowsTwice.milo` and `strShardWindowsTwice.milo` became the first such fixtures and Windows CI went red exactly as described. Both now carry `@skip-os: win32` pointing here; delete the skips when this is fixed. **The `std/seal` dodge does not generalise:** seal declares the four libc externs locally to avoid importing `std/os`, but `std/shard` reaches `std/os` transitively through `std/sync`, so only moving the externs out fixes it. **Fix:** move the OpenSSL externs from `std/os` into `std/tls` (or a `std/openssl` leaf) so `std/os` is a clean libc module, then drop seal's local declarations and the two skips. Touches `std/tls`, `std/fetch`, `std/ws`. | `std/os.milo:176-230`, `std/seal.milo`, `std/shard.milo`, `std/sync.milo` |

## Dependency notes

- **Byte views (#7) gate the zero-copy form of the JSON byte-feed (#8).** #8 works without it (materialize per event), but hands out copies until #7 lands.
- **The child-exit arm is the pattern to copy** for any event-driven child wait: `installSignalPipe(sigchld())` + `sel.onRead(fd)` + `waitpid(..., WNOHANG)` (`tests/fixtures/selectChildExit.milo`, and `examples/cli-tools/timeout.milo` for a real use incl. the fork/inherit hazards).
- **Compile-time reduction likely wants MIR (Tier 3)** for real wins — profile before committing.

## milojs backlog moved

The milojs entries (ES2023 change-by-copy methods, missing built-in `.prototype`)
live with the source now: <https://github.com/milo-language/milojs/blob/main/docs/backlog.md>.

## Language ergonomics: `for (i, x) in vec` tuple binding (user, 2026-07-20)

for-in binds a single name, so index+element loops stay C-style `while i < len()`.
Add tuple/destructuring binding: `for (i, x) in vec.enumerate()` (`.enumerate()`
already exists). Converts most `while i < .len()` loops to for-in. match already
destructures tuples, so the pattern machinery exists. Scope: parser + checker +
codegen bind the pattern per iteration.
