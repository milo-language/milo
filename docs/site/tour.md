---
# The lesson pager already sits at the bottom; a second prev/next pair under it reads as noise.
prev: false
next: false
---

<script setup>
// Imported here, not registered in the theme, so the lesson data ships with this page only.
import MiloLab from './.vitepress/theme/MiloLab.vue'
</script>

# Take Milo for a walk

Each program's output below is captured from a native build. To edit and run them yourself, [install Milo](/getting-started/installation).

<MiloLab />
