{
  "name": "milo-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "stage": "cp ../../install.sh public/install.sh",
    "dev": "bun run stage && vitepress dev",
    "build": "bun run stage && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "dependencies": {
    "feed": "^6.0.0"
  }
}
