# std/path

Path manipulation utilities.

```milo
from "std/path" import { Path }
```

## Example

```milo
print(Path.ext("archive.tar.gz"))            // .gz
print(Path.basename("/home/user/file.txt"))  // file.txt
print(Path.dirname("/home/user/file.txt"))   // /home/user
print(Path.join("/home/user", "docs"))       // /home/user/docs
print(Path.stem("report.pdf"))               // report
```

<!-- generated:api -->
<!-- Do not edit between these markers: generated by scripts/gen-std-docs.ts from 'milo api --json'. Edit the doc comments in std/path*.milo. -->

## API reference

### `Path`

```milo
pub struct Path
```

Stateless path operations grouped for discoverability.

#### `Path.basename`

```milo
fn Path.basename(path: &string): string
```

Final component of the path: `Path.basename("/home/user/file.txt")` is
"file.txt".

#### `Path.dirname`

```milo
fn Path.dirname(path: &string): string
```

Directory portion of the path: `Path.dirname("/home/user/file.txt")` is
"/home/user".

#### `Path.ext`

```milo
fn Path.ext(path: &string): string
```

File extension including the dot, or "" if there is none:
`Path.ext("archive.tar.gz")` is ".gz".

#### `Path.join`

```milo
fn Path.join(a: &string, b: &string): string
```

Join two segments with a single "/": `Path.join("/home/user", "docs")` is
"/home/user/docs".

#### `Path.stem`

```milo
fn Path.stem(path: &string): string
```

File name without its extension: `Path.stem("report.pdf")` is "report".

<!-- /generated:api -->
