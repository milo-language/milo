# std/sort

In-place sorting for common types.

```milo
from "std/sort" import { sortI64, sortStrings, reverseI64 }
```
## Example

```milo
var nums: Vec<i64> = [3, 1, 4, 1, 5]
sortI64(&mut nums)
// nums is now [1, 1, 3, 4, 5]

reverseI64(&mut nums)
// nums is now [5, 4, 3, 1, 1]
```

<!-- generated:api -->
<!-- Do not edit between these markers: generated by scripts/gen-std-docs.ts from 'milo api --json'. Edit the doc comments in std/sort*.milo. -->

## API reference

### `reverseI64`

```milo
pub fn reverseI64(v: &mut Vec<i64>): void
```

Reverse a Vec&lt;i64> in place.

### `sortI32`

```milo
pub fn sortI32(v: &mut Vec<i32>): void
```

Sort Vec&lt;i32> in ascending order.

### `sortI64`

```milo
pub fn sortI64(v: &mut Vec<i64>): void
```

Sort Vec&lt;i64> in ascending order.

### `sortStrings`

```milo
pub fn sortStrings(v: &mut Vec<string>): void
```

Sort Vec&lt;string> in lexicographic order.

### `sortStringsByFreq`

```milo
pub fn sortStringsByFreq(keys: &mut Vec<string>, vals: &mut Vec<i64>): void
```

Sort Vec&lt;string> + parallel Vec&lt;i64> by values descending, then keys ascending (for top-N).

<!-- /generated:api -->
